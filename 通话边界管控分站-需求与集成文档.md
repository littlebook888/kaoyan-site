# 通话边界管控话术系统 · 分网站需求与集成文档（V1.2）

> **归属项目**：考研个人网站管理系统（纯静态 HTML/CSS/JS，洛天依蓝 `#66ccff`，PWA 三端）
> **文档性质**：需求基线 + 分站集成方案 + 联动计时设计（供后续开发直接照做）
> **当前状态**：Excel 版话术系统已就绪（V1.2）；分站代码待开发
> **关联源文件（已归档）**：`call-boundary/通话边界管控话术系统V1.1.xlsx`（项目内归档副本，原对话目录副本见附录 A）；全部产物索引见文末「附录 A. 产物与文件索引」
> **维护方**：规则部（用户自设的个人规则体系）

***

## 0. 文档说明与「联动计时」定义（必读）

本文档把"通话边界管控话术系统"这一任务的**全部需求**集中起来，作为考研网站的一个**分网站（sub-site）的开发依据，并实现与主站的**联动计时。

**「联动计时」本方案的定义（双向）**，请审阅，若有偏差请指出,，**需要注意的是，分站的任何情况都不得破坏原有的主站的秩序，如果本文档与实主站开发有冲突，请遵循我们主站的开发文档！！：**

- **下行联动（主站 → 分站）**：分站实时读取主站 `active_timer` 会话状态。若主站正在学习计时，分站提示"你正在学习 X 分钟，通话将计入今日占用"，强化边界意识。
- **上行联动（分站 → 主站）**：分站开启"通话计时"时，通话时长作为一条时间记录写入主站 `time_records` 账本（`source = call_boundary`），主站的「今日三块 / 统计 / 热力图」自动体现"被通话占用的时长"。记录新标签【与林宇晨通话】
- **可选深度联动**：通话开始时，若主站正在学习，分站可一键暂停主站 `active_timer`

> ⚠️ 若你心中的"联动计时"仅指"分站显示主站考研倒计时数字"，请把上行/下行任一项去掉即可，其余不变。

***

## 1. 背景与目标

### 1.1 背景

福建医科大学放射医学大四学生，备战 2026 考研（306 西医综合，主跟天天师兄）。对方（林宇晨）**每天 随时来点**，且通话易无限拉长，侵占考研复习时间。需一套"人化、带歉意、有借口感"的话术 + 刚性计时（双闹钟）来守住边界。

### 1.2 目标

1. **来电一眼判定**：偶数日 / 奇数日 / 周频率下，该接还是该拒，速查即知。
2. **话术可一键复制**：每条拒接/开场/收尾话术单格完整，点一下就能粘贴。注意，话术需要有人样，需要礼貌（如请、麻烦、不好意思等等等）
3. **借口随机轮换**：避免固定套路被识破（6 款借口 + 多套随机池）。
4. **双闹钟刚性挂断**：接通瞬间起 15 分钟预警 + 25 分钟终极挂断，不靠意志力。
5. **周频率管控（8-20 起）**：聊天降频为「每周 1 次、最多 2 次」。
6. **与考研网站联动计时**：通话占用时间纳入主站时间账本，接电话前可见主站学习状态。

### 1.3 非目标

- 不替代主站计时器（分站是「第 ④ 类工具」，通过写 `time_records` 参与主线，不绕过 `Store` 直改 localStorage）。
- 不做手机原生"时钟 App"闹钟（Web 做不到，沿用 PWA 站内响铃）。
- 不做番茄钟 / 单块目标（用户已取消）。

***

## 2. 功能需求清单

| 编号   | 功能           | 优先级 | 说明                               |
| ---- | ------------ | --- | -------------------------------- |
| FR1  | 今日来电判定速查     | 必做  | 读 `clock.js` 判奇偶/周，输出"接/拒/可聊"    |
| FR2  | 话术速查 + 一键复制  | 必做  | 速查栏 / 排班表 / 随机池，点单元格复制           |
| FR3  | 随机借口抽取       | 必做  | 前端 `Math.random` 抽（不依赖 Excel 公式） |
| FR4  | 双闹钟计时（15/25） | 必做  | 接通起倒计时，到点响铃（复用 `ui.js`）          |
| FR5  | 排班表（日/周视图）   | 必做  | 8-12\~8-31 已排；9 月待续              |
| FR6  | 周频率管控        | 必做  | 8-20 起每周 1 次最多 2 次               |
| FR7  | 联动计时（双向）     | 必做  | 下行显示主站状态 + 上行写 `time_records`    |
| FR8  | 违规记录         | 建议  | 违规写入 `rule_break`，主站可见           |
| NFR1 | 纯静态零构建       | 约束  | 加库前先问"用户日后能自己改吗、离线能用吗"           |
| NFR2 | 三端可 PWA      | 约束  | 沿用 `manifest`/`sw.js` 体系         |
| NFR3 | 66ccff 视觉一致  | 约束  | 沿用 `theme.css` 变量，不另起炉灶          |

***

## 3. 完整规则集（系统灵魂，分站须严格遵守）

### 3.1 核心总则

1. **唯一接听/回拨窗口**：仅奇数日 而且自身事务处理完毕时，可按需接听或回拨当日未接来电。（不得影响正常学习）
2. **强制前置**：所有接通的语音通话全程必须开双闹钟（半程预警 + 终极挂断），严禁无时限畅聊。
3. **主动呼叫禁令**：复习/工作时段禁止主动拨打电话；回拨属可选项、非必须义务，主动权完全在己方。

### 3.2 分场景标准应答（8-20 前口径，历史保留）

- **偶数日来电（非窗口，必拒）**：直接挂断 → 发送「今天家里有事，急诊值班忙，改日再聊😊」等类似接口
- **奇数日非窗口来电（自身事务未完毕时）**：直接挂断 → 发送「今日急诊加班，回家后我回你电话」等类似接口

### 3.3 随机轮换借口库（6 款）

| 序号 | 类型         | 标准话术                     | 适用场景          |
| -- | ---------- | ------------------------ | ------------- |
| 1  | 手机电量类      | 手机快没电自动关机了，刚充上电才看到。      | 非窗口未接、隔夜回复    |
| 2  | 值班加班类      | 今天排班值班，要忙到晚上 9 点多，没空看手机。 | 白天/傍晚邀约、非窗口解释 |
| 3  | 急诊在岗类      | 在急诊科帮忙，手头事多，不方便接电话。      | 任意时段，通用性最强    |
| 4  | 病历工作类      | 在赶当天的病历和报告，得专心弄完，先不聊了。   | 窗口超时收尾、非窗口推脱  |
| 5  | 家庭事务类（奇专用） | 家里有点事要帮忙处理，走不开。          | 奇数日非窗口、窗口延长   |
| 6  | 休息状态类      | 昨晚太累很早就睡了，刚看到消息。         | 隔夜回复未接        |

> WPS 抽取公式（仅供 Excel 版）：`=INDEX(C:C,RANDBETWEEN(3,8))`；分站改用前端随机，见 §6.5。

### 3.4 未接来电回拨专项规则

- **合法前提**：仅奇数日、自身事务完毕时；单日来电 ≥3 次骚扰式呼叫，当日取消回拨资格。
- **标准流程**：①间隔 10\~20 分钟再拨（强化"刚忙完才有空"人设）；②统一开场；③接通瞬间双闹钟（15/25）；④标准收尾。
- **不回拨补处理**：次日补发「昨晚值班/赶材料弄到太晚，看到的时候已经很晚了，就没回你，有事你打字说就行。」

### 3.5 违规处罚机制

- **判定**：非窗口接听 / 通话未设闹钟超时畅聊 / 非规定时段主动回拨，均算违规。
- **处罚**：单次违规 → 取消后续 2 期奇数日语音接听资格（2 次禁言），仅保留极简文字回复。
- **执行**：处罚期内所有语音一律拒接，用借口库推脱，不得中途破例。
- **注意：违规处罚机制手动判断，不进行自动判断，**

### 3.6 执行铁律

1. 借口点到为止，不展开细节、不辩解、不道歉，不给对方讨价还价空间。
2. 借口随机轮换，避免固定套路被识破。
3. 通话全程必须开倒计时闹钟，到点刚性挂断，不靠意志力。
4. 处罚一旦触发严格执行；一次心软破例，边界全线崩溃。

### 3.7 8-20 规则升级（周频率）

- **生效日**：8-20。原因：电话隔离取得一定效果，降频为「每周 1 次、最多 2 次」。
- **计数**：自然周（周一\~周日）。默认主聊日 = 周日，可选加次 = 周四；8-20 当天算本周第 1 次。
- **聊天日**：可全天聊（仍须双闹钟）。**非聊天日**：全天拒接，17:30 统一用「本周已聊 / 约好日子」理由。

***

## 4. 话术库全集（分站数据底座）

> 分站前端应从一个静态 JSON 文件（`static/js/call-data.js`）读取以下全部话术，而非硬编码在 HTML。下面即数据底座内容。

### 4.1 速查栏（来电一眼可见）

- 偶数日来电 → 挂断 + 「今天家里有事，今天急诊，改日再聊😊」
- 奇数日（自身事务未完毕时）来电 → 挂断 + 「今日急诊加班，回家后我会打你电话」
- 唯一窗口：奇数日、事务完毕，可接/回拨
- 铁律：接通全程双闹钟

### 4.2 17:30 随机话术池（17 条，他每天 17:30 必来电）

```
1. 哎呀真不好意思，今天排班值班这会儿还没忙完，17:30正脱不开身，等我回家再打你哈😊
2. 不好意思啊今天家里有点状况走不开，17:30这会儿正忙着，等空了找你哈😊
3. 抱歉哈，今天急诊这会儿正忙得脚不沾地走不开，先不说了，等我忙完回家再找你聊哈
4. 哎呀真不好意思，今天排班值班这会儿还没忙完，17:30正脱不开身，等我回家再打你哈😊
5. 真不巧，今天病历/报告这会儿还没赶完，正专心弄，先不聊了哈，晚点回家我打你😊
6. 真不巧今天病历/报告这会儿正赶着，17:30走不开，晚点我打你哈😊
7. 抱歉哈，今天院里临时叫帮忙，这会儿正忙，17:30走不开，等我忙完找你哈😊
8. 不好意思啊，家里这会儿有点事得处理走不开，等我忙完回家再找你哈
9. 真不好意思今天有点累懵了，17:30刚看到你电话，不太方便接，改日哈😊
10. 不好意思啊今天家里有点事走不开，17:30这会儿正忙，等空了找你哈😊
11. 抱歉哈，今天急诊这会儿正忙得脚不沾地走不开，先不说了，等我忙完回家再找你聊哈
12. 抱歉哈，今天急诊这会儿正忙得脚不沾地，17:30走不开，等我忙完找你哈😊
13. 真不巧，今天病历/报告这会儿还没赶完，正专心弄，先不聊了哈，晚点回家我打你😊
14. 真不巧今天得赶材料，这会儿正弄着，17:30走不开，忙完我打你哈😊
15. 哎呀真不好意思，今天排班值班这会儿还没忙完，17:30正脱不开身，等我回家再打你哈😊
16. 抱歉哈今天值班走不开，17:30这会儿正忙着，回家我再回你电话哈😊
17. 不好意思啊，家里这会儿有点事得处理走不开，等我忙完回家再找你哈
```

> 奇日 17:30 用「忙完再聊」式拒接（不暴露窗口）；偶日 17:30 沿用当日拒接话术。抽到「手机电量/休息状态」日免排 17:30，仅备注睡觉/充电情况。

### 4.3 窗口日·开场随机池（6 条，接通瞬间）

```
1. 刚忙完急诊/病历的事才有空看手机，刚看到你之前打电话，找我啥？
2. 哎呀今天可算忙完了，刚看到你之前打的电话，有啥事呀？
3. 不好意思哈，刚把手头活儿弄完才看到你来电，找我啥事？
4. 刚交完班/写好病历才有空摸手机，刚看到你电话，咋啦？
5. 今天科室的活儿刚收尾，我才得空看你消息，找我啥？
6. 刚把事儿都忙完，才看见你打的电话，有啥要跟我说的？
```

### 4.4 窗口日·收尾随机池（6 条，到点挂断前）

```
1. 不早了，我得洗漱休息了，明天还得早起交班，有事你打字留言就行～
2. 哎呀都这个点了，我得去洗漱睡了，明早还得交班呢，有事先发我消息哈
3. 真不巧时间过这么快，我得准备睡了，明天一早要交班，有事你打字留给我
4. 不早啦，我得去洗漱了，明天还得早起，先不聊了哈，有事留言～
5. 得睡了，明天还得早起交班，就先到这吧，有啥你打字说就行
6. 时间不早了，我得休息了，明早交班得起得来，有事你发我就好哈
```

### 4.5 人化备选话术库（11 条，可随机替换）

```
【拒接·急诊/值班向】
- 真不好意思，今天院里临时有事走不开，本来想聊的，实在抽不出空，先不说了哈😊
- 哎呀今天急诊忙得脚不沾地，走不开，等我忙完这阵再找你聊哈
- 抱歉哈今天排班值班得弄到挺晚，先不说了，回家我再打给你
【拒接·家庭/事务向】
- 不好意思啊今天家里有点状况得处理，腾不出手，等空了找你哈
- 真不巧家里有点事走不开，今天不太方便接，改日咱们好好聊😊
【拒接·病历/报告向】
- 真不巧今天得赶完病历和报告，得专心弄，先不聊了，忙完找你哈
- 抱歉哈今天得赶材料，弄完再聊，先不说了哈😊
【拒接·疲惫/休息向】
- 真不好意思今天有点累懵了刚看到消息，不太方便接，改天哈
- 哎呀今天状态不太行，刚看到你消息，先歇了，有事你打字留给我哈
【窗口日·开场】刚忙完急诊/病历的事才有空看手机，刚看到你之前打电话，找我啥？
【窗口日·收尾】不早了，我得洗漱休息了，明天还得早起交班，有事你打字留言就行～
```

### 4.6 周频率拒接话术（8-20 起，非聊天日用）

- 额度已用：「真不好意思啊，这周咱们刚聊过，今天手头事一堆走不开，先不说了哈，等约好的日子再好好聊😊」
- 本周未聊：「新的一周啦/今天手头事多走不开，这周咱们约好的日子再聊哈😊」
- 聊天日：「今天刚好有空，咱们好好聊会儿～不过我得定个闹钟，待会还有事要弄😊」

***

## 5. 排班逻辑

### 5.1 奇偶日历史（8-12 \~ 8-19，已执行/作为记录）（AI生成，仅供参考）

- 8-12（周三）畅聊（执行前参考）
- 8-13（周四·奇）跳过窗口，全天拒接
- 8-14（周五·偶）拒接 ｜ 8-15（周六·奇）窗口 ｜ 8-16（周日·偶）拒接
- 8-17（周一·奇）窗口 ｜ 8-18（周二·偶）拒接 ｜ 8-19（周三·奇）窗口（休息免排 17:30）

### 5.2 周频率（8-20 起）

- 自然周（周一\~周日）；每周 1 次为主、最多 2 次。
- 默认主聊日 = 周日，可选加次 = 周四；8-20 算本周第 1 次。

如果本周已经超过2次了，在开始计时之前提示：本周已经超过2次…………，进行提醒和警告，当然，我（用户）也有两个选择，一个是继续畅聊，一个是返回学习（）进行拒绝。

### 5.3 周频率聊天日总览（8-20 起）（AI生成，仅供参考）

| 周次  | 日期范围         | 主聊天日①    | 可选加次②    | 说明                  |
| --- | ------------ | -------- | -------- | ------------------- |
| 第1周 | 8-17 \~ 8-23 | 8-20(周四) | 8-23(周日) | 新规 8-20 生效，本周已用 1 次 |
| 第2周 | 8-24 \~ 8-30 | 8-30(周日) | 8-27(周四) | 每周 1 次为主；如需第 2 次选周四 |
| 第3周 | 8-31 \~ 9-6  | 8-31(周一) | —        | 进入新周，主聊日自行定         |

> ⚠️ **9 月排班待续**：第 3 周仅排到 8-31，9-1 起需按同规则延续（建议做成可配置 JSON，分站自动算自然周）。

***

## 6. 分网站集成方案（AI生成，仅供参考）

### 6.1 定位

分站是主站「第 ④ 类工具」：通过写 `time_records`（新 `source`）参与主线，**不绕过** **`Store`** **直改 localStorage，不改** **`active_timer`** **之外的他人数据**。

### 6.2 文件与路由

- **放根目录同级**（避免破坏相对路径）：新增 `call.html` + `static/js/call.js` + `static/js/call-data.js`。
- **导航**：4 个 HTML 各自的 `<nav class="tabbar">` 里都加一项「边界」（改 4 处，无共享模板）：
  ```html
  <a href="call.html"><span class="ic" data-icon="phone"></span>边界</a>
  ```
- **脚本加载顺序**（沿用四页一致）：`config.js → blocks.js → clock.js → store.js → supabase CDN → icon.js → reveal.js → langqian.js → ui.js → call.js`
- ⚠️ **相对路径陷阱**：`icon.js` 用 `BASE="assets/icons/"`、各 `<script src="static/js/...">`、manifest、音频全是相对路径。**分站放根目录同级即可，勿放子目录**（否则全 404）。

### 6.3 UI（沿用设计系统）

- 复用 `theme.css` 的 `:root` 变量（`--accent:#66ccff`、`--card`、`--radius-lg` 等），不另起样式。
- 页面结构：顶部洛天依蓝光晕 + `.bg-ribbon` 缎带（抄 `index.html` 的 `<body>` 首元素）+ 卡片式模块。
- 字体沿用系统字体栈。

### 6.4 功能模块（call.js）

1. **今日判定卡**：读 `window.Blocks` / `clock.js` 判奇偶与周；输出「接 / 拒 / 可聊」+ 推荐借口类型。
2. **话术速查 + 一键复制**：渲染速查栏、排班表；点单元格 `navigator.clipboard.writeText`。
3. **随机借口按钮**：`call-data.js` 里按类型随机抽一条，点「换一条」刷新。
4. **双闹钟倒计时组件**：接通时启动 15min 预警 + 25min 终极；复用 `window.UI.beep/buzz/notify`（见 §7.5）。
5. **排班视图**：读 `call-data.js` 的排班 JSON，渲染日/周视图；高亮今日。

### 6.5 数据层（`static/js/call-data.js`）

纯静态 JSON，结构示例：

```js
window.CALL_DATA = {
  excuses: [ /* §3.3 六款 */ ],
  pool1730: [ /* §4.2 十七条 */ ],
  poolOpen: [ /* §4.3 六条 */ ],
  poolClose: [ /* §4.4 六条 */ ],
  altLibrary: [ /* §4.5 十一条 */ ],
  weeklyPlan: [ /* §5.3 三周 */ ],
  // 9月排班生成规则（自然周，主聊日=周日，加次=周四）
  rule: { weekStart: 'mon', mainChat:'sun', addChat:'thu', since:'2026-08-20' }
};
```

***

## 7. 联动计时设计（核心）（AI生成，仅供参考）

### 7.1 架构

```
[考研主站 index/timer/tasks/stats]
        │ 共享层：localStorage(kaoyan:*) + BroadcastChannel(kaoyan-sync) + Supabase(待启用)
        │
[分站 call.html]
   ├─ 今日判定(奇偶/周)      ← clock.js / blocks.js
   ├─ 话术速查+复制          ← call-data.js
   ├─ 随机借口               ← Math.random
   ├─ 双闹钟倒计时(15/25)    ← ui.js (beep/buzz/notify)
   ├─ 排班视图(周频率)       ← call-data.js
   └─ 联动计时
        ↓ 下行  Store.subscribeActiveTimer  → 显示主站学习状态
        ↑ 上行  通话计时结束 → Store.addTimeRecord(source=call_boundary)
        ↔ 可选  通话暂停/恢复主站 active_timer
```

### 7.2 数据模型

- **复用** **`active_timer`**（单行会话）：分站读它做下行展示；不自行新建冲突会话。
- **`time_records`** **新增来源标记**：通话记录写 `source = 'call_boundary'`，主站统计自动归类。
- **`config.js`** **的** **`TIME_CATEGORIES`** **新增一级分类**（建议）：
  ```js
  { key:"call", label:"通话边界", color:"#66ccff", icon:"phone",
    countTowardGoal:false,   // 通话不计入学习目标
    subs:[ {key:"linyuchen", label:"林宇晨"},
           {key:"rule_break", label:"违规"},
           {key:"rule_analyze", label:"规则分析"} ] }
  ```
  > 注：现有 `other` 下已有 `rule_analyze`/`rule_break`，可迁移到 `call` 下或保留。
- **`sql/schema.sql`** **无需改**：`time_records` 已支持任意 `category`/`source`，且无新增列（沿用现有字段）。

### 7.3 下行联动（主站 → 分站）

```js
// call.js 加载时
const at = window.Store.getActiveTimer();
renderHostStatus(at);  // 若 at.kind==='study' && at.status==='running' → 显示"正在学习 X 分钟"
window.Store.subscribeActiveTimer((val) => renderHostStatus(val));
```

> 接电话前分站提示："你正在学习 X 分钟，通话将计入今日占用"——强化边界意识。

### 7.4 上行联动（分站 → 主站）

```js
// 通话结束（双闹钟终极挂断或手动结束）时
const dur = Math.round(currentCallElapsed()); // 分站自有正计时（复用 timer.js 的 currentElapsed 算法）
window.Store.addTimeRecord({
  id: uid(), user_id: C.USER_ID,
  category: "call", sub_category: "linyuchen",
  label: "与林宇晨通话",
  tags: ["边界管控"],
  started_at: new Date(callStartedAt).toISOString(),
  ended_at: new Date().toISOString(),
  duration_sec: dur,
  source: "call_boundary",
  note: "双闹钟 " + (violated ? "超时违规" : "正常挂断")
});
```

> 主站「今日三块 / 统计 / 热力图」自动体现通话占用，无需改主站代码。

**可选深度联动**：通话开始时若主站 `active_timer` 是 study 且 running，分站调 `Store.setActiveTimer({...at, status:'paused'})` 暂停；结束恢复 `status:'running'`。标注为可选 Phase，谨慎启用（避免与主站计时器状态混淆）。

### 7.5 双闹钟实现（复用 `ui.js`）

- 分站启动两段倒计时：15min（预警）→ `UI.beep(1); UI.notify("⏰ 还有10分钟，准备收尾", ...)`；25min（终极）→ `UI.beep(3); UI.buzz(); UI.showAlert("到点了，刚性挂断", 5000); UI.notify("⏰ 通话结束", ...)`。
- 依赖 `ui.js` 已实现的 `beep/buzz/showAlert/notify`（Web Audio + 振动 + 系统通知）。
- ⚠️ 局限性：系统通知是页面内 `new Notification`，**页面被杀/手机锁屏后不响**；正式上线需注册 `sw.js` 用 SW 通知（见 §8 坑③）。

### 7.6 跨设备同步

- 依赖 Supabase（免费版 Realtime）。**当前** **`Store.initSupabase()`** **从未被调用**，需先补调用（见 §8 坑②），否则跨设备联动不生效，仅同浏览器多标签可用（`BroadcastChannel`）。

***

## 8. 技术约束与已知坑（来自对考研项目的探索，必看）（AI生成，仅供参考）

| # | 坑                              | 影响                          | 处理                                                                                             |
| - | ------------------------------ | --------------------------- | ---------------------------------------------------------------------------------------------- |
| ① | **相对路径陷阱**                     | 分站放子目录会 404                 | 分站放根目录同级（`call.html`）                                                                          |
| ② | **`initSupabase()`** **从未被调用** | 跨设备同步永不生效                   | 在入口补 `Store.initSupabase().then(()=>Store.pullOnce())`                                         |
| ③ | **`sw.js`** **从未注册**           | PWA 离线/锁屏响铃失效               | 在 4 个 HTML 末尾 `navigator.serviceWorker.register('sw.js')`                                      |
| ④ | **`active_timer`** **表缺 6 列**  | 启用 Supabase 后 upsert 静默 400 | `sql/schema.sql` 给 `active_timer` 补 `tags/sub_category/segments/first_started_at/task_id/note` |
| ⑤ | **三系统边界铁律**                    | 分站越权改数据                     | 只写 `time_records`，不直改 localStorage，不碰他人 `active_timer`                                         |
| ⑥ | **纯静态零构建**                     | 加框架需谨慎                      | 保持原生 JS；加库前问"用户能否自改、离线能否用"                                                                     |
| ⑦ | **改静态资源须升** **`CACHE`** **版本** | 用户刷新看到旧版                    | 改 `call.html`/`call.js` 后，`sw.js` 的 `CACHE="kaoyan-vXX"` 升号                                    |

***

## 9. 实施步骤（MVP → 完整）（AI生成，仅供参考）

- **Phase 0（前置修复）**：修坑②（`initSupabase` 调用）+ 坑④（`active_timer` 补列）+ 坑③（`sw.js` 注册）。
- **Phase 1（分站骨架）**：`call.html` + `call-data.js` + `call.js`；导航加「边界」；话术速查 + 一键复制。
- **Phase 2（计时与借口）**：双闹钟倒计时（复用 `ui.js`）；随机借口按钮；排班日视图。
- **Phase 3（联动计时）**：下行显示主站状态；上行通话写 `time_records(source=call_boundary)`；`config.js` 加 `call` 分类。
- **Phase 4（周频率 + 9月）**：排班周视图；9 月排班 JSON 自动生成（自然周规则）。

***

## 10. 术语表与参考（AI生成，仅供参考）

- **主站系统**：考研个人网站管理系统的计时/任务/统计模块（详见其 `交接文档.md`）。
- **active\_timer**：主站单行活动计时会话（localStorage + Supabase），三端同控。
- **time\_records**：主站时间账本（所有时间块真相来源），通话记录写入此处。
- **Store**：主站数据层（`window.Store`），提供 `getActiveTimer/setActiveTimer/subscribeActiveTimer/addTimeRecord` 等。
- **双闹钟**：15min 预警 + 25min 终极挂断。
- **周频率**：8-20 起，每周聊天 1 次最多 2 次（自然周计）。

**参考文件**

- 源 Excel：`D:\Agentwork\workbuddy\2026-08-12-22-11-36\通话边界管控话术系统V1.1.xlsx`
- 主站项目：`D:\Agentwork\workbuddy\考研个人网站管理系统\`（README.md / 交接文档.md / sql/schema.sql / static/js/timer.js,store.js,ui.js,config.js / theme.css）

***

## 附录 A. 产物与文件索引（供 AI / 协作者直接定位）（AI生成，仅供参考）

> **目的**：本任务所有产物已归档进考研网站项目内的 `call-boundary/` 子目录，与本文档同树。其他 AI 或协作者读取本文档后，可凭下表路径直接**定位、读取、复用**全部文件，无需追问用户。
> **路径约定**：相对路径以 `考研个人网站管理系统/`（项目根）为基准；绝对路径为 Windows 本地路径。

### A.1 本任务核心产物

| 文件               | 相对路径（项目根）                           | 绝对路径                                                                  | 类型       | 用途                                                       | 状态         |
| ---------------- | ----------------------------------- | --------------------------------------------------------------------- | -------- | -------------------------------------------------------- | ---------- |
| 分站需求与集成文档        | `通话边界管控分站-需求与集成文档.md`               | `D:\Agentwork\workbuddy\考研个人网站管理系统\通话边界管控分站-需求与集成文档.md`               | 需求文档     | 本文件：全部规则 / 话术 / 分站方案 / 联动计时设计基线                          | ✅ 当前       |
| 话术系统 Excel（V1.2） | `call-boundary/通话边界管控话术系统V1.1.xlsx` | `D:\Agentwork\workbuddy\考研个人网站管理系统\call-boundary\通话边界管控话术系统V1.1.xlsx` | Excel 表格 | 5 工作表：①速查栏 ②核心规则 ③借口库 ④回拨·处罚·铁律 ⑤8月排班表（含17:30专列 + 3个随机池） | ✅ 当前（归档副本） |
| 话术系统 Excel（原始副本） | —（项目树外）                             | `D:\Agentwork\workbuddy\2026-08-12-22-11-36\通话边界管控话术系统V1.1.xlsx`      | Excel 表格 | 对话工作目录中的原始生成副本，内容与归档副本一致                                 | ✅ 同步       |

### A.2 可复现构建脚本（归档于 `call-boundary/`）（AI生成，仅供参考）

> 修改规则后重跑对应脚本即可重新生成 xlsx（脚本内已写死输出路径，会就地覆盖）。环境：Python 3.13 虚拟环境 + `openpyxl`。

| 脚本                     | 相对路径                                 | 用途                              |
| ---------------------- | ------------------------------------ | ------------------------------- |
| `build_table.py`       | `call-boundary/build_table.py`       | V1.0 基础：生成 速查/规则/借口库/回拨处罚铁律 4 表 |
| `build_schedule.py`    | `call-boundary/build_schedule.py`    | 追加 ⑤8月排班表（奇偶逻辑 + 人化话术）          |
| `build_1730.py`        | `call-boundary/build_1730.py`        | 追加 ⑥17:30 奇数日专表（后并入 ⑤）          |
| `build_merge.py`       | `call-boundary/build_merge.py`       | 合并 ⑥→⑤，重做 ⑤ 含 17:30 专列 + 随机池    |
| `build_window_pool.py` | `call-boundary/build_window_pool.py` | 追加窗口日「开场 / 收尾」随机池               |
| `update_v12.py`        | `call-boundary/update_v12.py`        | V1.2：8-20 起改周频率 + 周频率总览         |

### A.3 分站依赖的主站文件（上下文，非本任务产物）

| 文件   | 相对路径                    | 与分站关系                                                   |
| ---- | ----------------------- | ------------------------------------------------------- |
| 入口页面 | `index.html`            | 导航加「边界」入口 → `call.html`                                 |
| 计时器  | `timer.html`            | 联动计时数据源（`active_timer` / `time_records`）                |
| 数据层  | `static/js/store.js`    | 分站读 `active_timer`、写 `time_records` 的接口（`window.Store`） |
| 计时逻辑 | `static/js/timer.js`    | 双闹钟倒计时可复用                                               |
| 配置   | `static/js/config.js`   | 加 `call` 分类                                             |
| 样式   | `theme.css`             | 66ccff 洛天依蓝配色                                           |
| 数据库  | `sql/schema.sql`        | `active_timer` 补 6 列（见 §8 坑④）                           |
| 主站说明 | `README.md` / `交接文档.md` | 主站背景与现状                                                 |

### A.4 一键定位（绝对路径速查）（AI生成，仅供参考）

```
分站文档 : D:\Agentwork\workbuddy\考研个人网站管理系统\通话边界管控分站-需求与集成文档.md
话术xlsx : D:\Agentwork\workbuddy\考研个人网站管理系统\call-boundary\通话边界管控话术系统V1.1.xlsx
脚本目录 : D:\Agentwork\workbuddy\考研个人网站管理系统\call-boundary\
主站项目 : D:\Agentwork\workbuddy\考研个人网站管理系统\
```

