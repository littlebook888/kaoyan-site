# 考研个人网站管理系统 · 交接文档（v2）

> **给接手的 AI / 开发者（含 ChatGPT）**：请**完整读完本件**再动任何代码。
> **版本基线：v1.12.1（2026-09-15）**｜本文档为唯一权威交接件（旧版 v1.5.1 的内容已全部并入本件，历史版本见 git）
> ⚠️ 本件随仓库公开（GitHub Pages），**已清理全部个人信息**；后续增改请勿写入学校/学号/真实姓名/本机用户名/个人路径。

---

## 0. 30 秒速览 & 阅读顺序

**这是什么**：一个考研学生的个人时间管理系统。纯静态 HTML/CSS/JS（**零框架、零构建、零依赖**），Supabase 做跨设备同步，手机/电脑/平板三端 + PWA。

**接手前必读顺序**：第 1 节（隐私纪律）→ 第 5 节（核心机制）→ 第 6 节（硬规则）→ 第 7 节（踩坑史）→ 第 9 节（测试方法论）。
要开发新功能，再看第 10 节（已批准的「每日复盘」完整方案）。

**线上地址**
- 主站：`https://littlebook888.github.io/kaoyan-site/`
- 刷题站（子目录）：`https://littlebook888.github.io/kaoyan-site/neike-306-tiku/`

**本地预览（必须用 HTTP 服务，不能 `file://` 直开）**
```bash
cd "项目根目录"
python -m http.server 8099 --bind 127.0.0.1
# 浏览器打开 http://127.0.0.1:8099/index.html
```

**规模**：7 个页面 · 24 个 JS 文件（约 9,800 行）· 1 个 CSS（约 2,700 行）· 数据库 7 张表

---

## 1. ⚠️ 隐私与账号纪律（最高优先级）

### 1.1 账号矩阵

| 账号 | 用途 |
|---|---|
| **littlebook888**（主号） | **本网站**的 GitHub 账号 |
| **xiaobenzic888**（小号） | 打印站专用（**与本项目无关，不要混用**） |

### 1.2 绝不能写进仓库的内容

- 学校、学号、身份证、手机号、专业年级
- 第三方**真实姓名**（通话边界相关，见 1.3）
- 本机 **Windows 用户名 / 个人绝对路径**（如 `C:\Users\<用户名>\...`）
- 提交作者必须是隐私邮箱 `littlebook888@users.noreply.github.com`（**不要**出现 QQ 邮箱）

### 1.3 已脱敏 / 有意保留（★ 别"好心"改回去）

| 项 | 状态 | 说明 |
|---|---|---|
| 通话对象显示名 | **已脱敏**（v1.9.0） | 全站统一显示「通话」「通话对象」；**数据键 `linyuchen` 必须保留**（历史记录靠它关联，改键会断数据） |
| 通话借口的**职业人设** | **有意保留，勿脱敏** | 借口里的「值班/加班/急诊/病历/交班/手术/夜班」等是**用户明确要求保留并强化**的——现实中确为医学实习、常加班处理急诊，这类"职业义务型"理由最不易被对方纠缠。`static/js/call-data.js` 文件头有同样的提示。**不要以"隐私"为由改成通用表述** |
| 一句话记忆 | — | 姓名是**别人的**隐私（要脱敏）；职业人设是**用户自己的**挡箭牌（用户决定保留） |

### 1.4 搜索引擎收录风险（2026-09-13 实测）

- 站点**无 robots.txt / 无 noindex** → 可被自由抓取
- 当时实测尚未被收录，但**收录只是时间问题**
- **git 历史仍保留旧文本**（工作区清理不影响历史提交）。如需彻底清除须重写历史（`git filter-repo` + 强制推送）——**破坏性操作，必须用户确认后再执行**

---

## 2. 项目是什么

### 2.1 定位

面向考研学生的个人时间管理系统。核心心智模型是**「按三餐切分的大块学习」**（早块/午块/晚块）+ **时间账本**（对标爱时间/时间日志）+ **任务打卡**（对标番茄ToDo）。目标用户（用户本人）习惯：边用边改、按需微调规则。

### 2.2 三套系统边界（不可混淆）

| 系统 | 职责 | 数据流向 |
|---|---|---|
| ① **时间记录系统**（主线） | `time_records` 账本，唯一真相来源 | 所有计时结果写入这里 |
| ② **计时器系统** | 正计时（打点）/ 倒计时工具 | **写入** ① |
| ③ **三块展示系统** | 早/午/晚三卡 + 进度条 | **只读** ①（不得改写非 meal 类数据） |

现有独立任务块（互不影响，各自 source 隔离）：**西综计划** `xizong_plan`、**生理滚动复习** `physio_rolling`（日期解耦）、**英语单词突围每日计划** `english_words`（按日期推进）。

### 2.3 页面清单

| 页面 | 职责 | 关键脚本 |
|---|---|---|
| `index.html` | 首页：实时时钟 / 考研倒计时 / **现在该做什么（规则部时间表）** / 今日三块 / 今日活动构成（饼图·时钟·时间轴·列表）/ 快速计时 / 链接大全 | home-schedule.js + home.js |
| `timer.html` | 计时器：正计（打点）/ 倒计 / 进入状态模式 / 标签抽屉 / 放音乐 / 休息收心预警 / 调试控制台 | timer.js |
| `tasks.html` | 任务：日历·列表·方格·课程表四视图 + **单词突围独立卡** + 生理滚动复习独立卡 | tasks.js |
| `stats.html` | 统计：核心数据 / 学霸指数 / 热力图 / 分类明细 / 任务报表 / CSV 导出 | stats.js |
| `call.html` | 通话边界：当前时段 / 今日判定 / 主站状态 / 话术 / 借口库 / 双闹钟 / 今日通话分钟 / 规则部豁免选择窗口 | call.js + call-data.js |
| `reminders.html` | 提醒栏目：晨启宣言 + 6 场景 18 卡 + 规则部速查（浪前/规则部原文，逐字保真） | reminders.js + reminders-data.js |
| `schedule.html` | 时间表：北京时间联动 + 理论 vs 实际对比 + 规则速查 + 心态工具 | schedule.js + schedule-data.js |

### 2.4 各页脚本加载顺序（**勿乱**，新文件按此相对位置插）

```
index:     config → blocks → clock → store → today-records → [supabase CDN] → icon → reveal → ui
           → schedule-data → home-schedule → home
timer:     config → blocks → clock → store → [supabase] → icon → reveal → ui → schedule-data → timer
tasks:     config → blocks → clock → store → timer → [supabase] → [xlsx] → icon → reveal → ui
           → schedule-data → xizong-plan → xizong-physio → xizong-live → today-xizong-plan → word-plan → tasks
stats:     config → blocks → clock → store → today-records → [supabase] → icon → reveal → ui → schedule-data → stats
call:      config → blocks → clock → store → [supabase] → icon → reveal → ui → timer → schedule-data → call-data → call
reminders: config → blocks → clock → icon → reveal → ui → reminders-data → reminders
schedule:  config → blocks → clock → store → [supabase] → icon → reveal → ui → schedule-data → schedule
```
> 纯数据文件（`*-data.js`、`*-plan.js`）必须在使用它的业务脚本**之前**加载。
> `reminders.html` 特意不加载 store.js（纯静态栏目）；`timer.js` 在 call 页以**无头模式**运行（只暴露 API，不绑 UI）。

---

## 3. 技术形态与不可违背的设计原则

| 原则 | 说明 |
|---|---|
| **纯静态、零构建** | 无 npm/webpack/vite/框架。加任何依赖前先问：用户日后能直接改吗？离线能用吗？ |
| **全免费** | GitHub Pages + Supabase 免费档 + 本地化 SVG 图标（Lucide，MIT） |
| **用户能自己改** | 所有可调参数集中在 `static/js/config.js`（作息/分类/标签/目标/主题色） |
| **时间判定只走 blocks.js / clock.js** | 业务代码**禁止**直接 `new Date().getHours()` 判块或判日；一律用北京时间口径 |
| **读写数据只走 `window.Store` API** | 禁止直读 localStorage |
| **统计只走 `TodayRecords`** | 见 5.2，禁止各页自己写一套 |
| **洛天依只做风格致敬** | 仅 #66ccff 主色 + 原创 SVG；**严禁**搬运官方立绘/歌曲/版权图 |
| **不搬运版权内容** | 浪前/规则部内容为逐字摘录的用户自有资料（已确认） |

---

## 4. 文件地图（全部文件 · 行数 · 职责）

### 4.1 页面（根目录，7 个）

`index.html` `timer.html` `tasks.html` `stats.html` `call.html` `reminders.html` `schedule.html`
> 每页 `<head>` 第 7 行有 `<meta name="app-version">`，**升级版本号必须 7 页同步改**（页面顶部有版本自检脚本：版本不符会清 SW 缓存并刷新）。

### 4.2 `static/js/`（24 个文件 · 共约 9,800 行）

| 文件 | 行数 | 职责 |
|---|---|---|
| `config.js` | 136 | ⭐ 配置中心：版本号 / Supabase / 作息边界 / 分类体系（12 类）/ 主题色 / 常用标签（14 个） |
| `blocks.js` | 97 | ⭐ 大块判定 + 北京时间工具（`beijing`/`secOfDay`/`dateStr`/`blockOf`/`currentKey`/`remainingSeconds`/`blockDurationSec`） |
| `clock.js` | 137 | ⭐ 全站顶部实时北京时间时钟 + **「现在该做什么」计划提示芯片**（状态行右侧） |
| `store.js` | 1153 | ⭐⭐ **数据层/同步底座**（详见 5.1） |
| `today-records.js` | 136 | ⭐⭐ **「今日记录」统一统计口径**（详见 5.2） |
| `ui.js` | 126 | 共享 UI：静音/图书馆模式、同步徽标、警报、响铃、震动、通知 |
| `icon.js` | 52 | 本地 SVG 图标注入（`data-icon="name"` → `assets/icons/name.svg`） |
| `reveal.js` | 33 | 滚动入场动画（IntersectionObserver，零依赖） |
| `home.js` | 1244 | 首页：三块 / 饼图 / 时间轴 / 时钟图 / 列表 / 记录编辑抽屉 / 补记 / 一键进入自习 |
| `home-schedule.js` | 306 | 首页「现在该做什么」卡：时段 hero + 一键正计时 + 分类提醒 + 规则速查 |
| `timer.js` | 2073 | 计时器核心：数字输入 / 正倒切换 / 标签抽屉（含起止时间编辑）/ 进入状态 / 音乐 / 收心预警 / `window.Timer` API |
| `tasks.js` | 1672 | 任务页：四视图 / 三个计划导入 / 单词突围独立卡 / 生理滚动复习独立卡 / 交互委托 |
| `stats.js` | 315 | 统计页：核心数据 / 学霸指数 / 热力图 / 分类明细 / 任务报表 / 两份 CSV 导出 |
| `call.js` | 894 | 通话边界核心逻辑（详见 5.7） |
| `call-data.js` | 117 | 通话话术数据底座（借口库 13 条 / 话术池 / 周规则 / 豁免配置） |
| `reminders-data.js` | 231 | 提醒栏目数据（晨启宣言 + 18 卡，**quote 必须逐字保真**） |
| `reminders.js` | 147 | 提醒栏目渲染（highlight 精确子串匹配） |
| `schedule-data.js` | 76 | ⭐ 规则部时间表数据（9 时段 + 6 条规则 + 6 个心态工具）——**主站与副站共用** |
| `schedule.js` | 175 | 时间表页渲染（当前时段高亮 / 理论 vs 实际 / 规则 / 心态） |
| `word-plan.js` | 56 | ⭐ 英语单词突围计划数据（41 天，规则见 5.5） |
| `xizong-plan.js` | 554 | 西综计划数据（61 条日计划） |
| `xizong-physio.js` | 35 | 生理滚动复习数据（43 天，循环生成） |
| `xizong-live.js` | 35 | 青云小阁官方计划本地镜像（4 天） |
| `today-xizong-plan.js` | 29 | 单日快照（供「从网站获取」） |
| `langqian/langqian.js` | 44 | 浪前「成长角落」占位内容（已由提醒栏目取代，保留作历史） |

### 4.3 其他

| 路径 | 说明 |
|---|---|
| `static/css/theme.css` | **全部样式**（约 2,700 行）。苹果风玻璃卡片 + 洛天依蓝 |
| `sw.js` | Service Worker：离线缓存（**改静态资源必须升 `CACHE` 版本号**，当前 `kaoyan-v118`；`SHELL` 数组列出预缓存文件） |
| `manifest.webmanifest` | PWA 清单（含 PNG 图标 192/512 + maskable） |
| `sql/schema.sql` | Supabase 建表 + 幂等补列（**可重复执行**，改动见 8.4） |
| `assets/` | `icon.svg` + `icon-192/512/maskable.png` + `icons/`（约 80 个 Lucide SVG）+ `study_start.mp3` |
| `.github/workflows/deploy-pages.yml` | 部署工作流（主站 + 刷题站一起构建发布） |
| `neike-tiku/` | 刷题站源码（React + Vite，构建产物发布到 `/neike-306-tiku/`） |
| `通话边界管控分站-需求与集成文档.md` | 通话边界的**需求基线**（V1.2，含规则集与话术库全集） |
| `docs/` | 早期文档（题库复刻计划/评估等） |
| `.dev-tools/` | **本机工具（已 gitignore，不入仓库）**：含协议注册 `.reg` 等 |

---

## 5. 核心机制（改代码前必须理解）

### 5.1 数据层 `store.js`（最复杂的文件）

**表与数据键**
- `active_timer`：**单行表** = 全局唯一「正在跑的计时器」
- `time_records`：历史流水（主线账本）
- `tasks` / `events` / `goals` / `study_sessions`（旧表兼容）

**写入链路**：`setLocal(key, value)` → ① localStorage ② BroadcastChannel（同浏览器多标签）③ Supabase（跨设备，Broadcast 操作通道 + Postgres CDC + 轮询三通道）

**关键机制**（都在 `store.js`，改前务必读懂）
| 机制 | 位置 | 作用 |
|---|---|---|
| `DATA_EPOCH = 3` | `store.js:23` | 数据纪元（见 5.4） |
| `_writeChain` 串行化 | — | 同一时刻只有一个 active_timer 写入 |
| `version` 单调递增 | — | 冲突检测（LWW-Register） |
| `op_id` 幂等去重 | — | 重复操作不重复应用 |
| 防回环四件套 | — | 心跳只写本地 / 远端空 15s 宽容期 / 停止保护窗 10s / `_stoppedByRemote` 墓碑 |
| **脏窗口守卫** | `store.js:613-617` `LOCAL_DIRTY_SKIP_MS=120s` | ⭐ 本地未推送的写入期间**禁止全量拉取覆盖本地**；拉取前 + `await` 返回落地前**双重检查**（见 7.1） |
| **缺列自愈 upsert** | `store.js:853` `_missingColumnName` / `store.js:862` `upsertRows` | ⭐ 云端缺列时自动剥离该列重试并告警（见 7.2） |
| 数组表删除 | `store.js:933` `pushDeleteRow` | 本地删完必须删云端行，否则 10s 轮询会把已删记录拉回来 |
| 旧表迁移 | `store.js:767` `migrateOldSessions` | `study_sessions` → `time_records`（time_records 为空时触发） |
| 省流轮询 | — | 数据轮询 10s 只做**行数探测**（约 100B），行数没变不拉全量；每 5 分钟强制全量；**页面切后台暂停一切轮询**（免费版 egress 5GB/月，曾一周见底） |

**Store 公开 API**（`window.Store`，`store.js:1137`）：
`isCloud / pullOnce / getLocal / setLocal / initSupabase` · `getActiveTimer / setActiveTimer / subscribeActiveTimer / isSyncing / subscribeSyncStatus` · `getTimeRecords / addTimeRecord / updateTimeRecord / deleteTimeRecord / subscribeTimeRecords` · `getTasks / addTask / updateTask / deleteTask / addFocusToTask / subscribeTasks` · `getSessions / addSession` · `getEvents / addEvent` · `getGoals / setGoals` · `setLog`

### 5.2 统计口径 `today-records.js`（唯一真相来源）

**所有"今日"统计必须走 `TodayRecords.getTodayRecords()`**（home.js / stats.js 共用）。

内部**五步严格执行**（`today-records.js:38`）：
1. `id` 去重（防同步/导入重复）
2. 与今日 `[00:00, 24:00)` **求交集**（跨天记录只计今日部分）
3. 真实时长：**优先 segments**（暂停分段求和），否则按跨度纠偏（差值 >60s 视为脏数据）
4. 单条今日时长 **>8h 截断**（防异常值撑爆统计）
5. **重叠区间并集合并**（防重复计时总和 >24h）

**绝不能**用 `isSameDay(started) && isSameDay(ended)` 过滤——会漏跨天记录（曾致统计超 24 小时）。
`segDurSec(raw, winS, winE)`（`:22`）是分段求交集的核心，可复用。

### 5.3 时间判定 `blocks.js` + `clock.js`

- 全站时间基准 = **北京时间（UTC+8）**，与设备时区无关（`beijing()` 换算后取值）
- `secOfDay()` 返回北京当天已过秒数；`dateStr()` 返回北京当天 `YYYY-M-D`
- 大块：`blockOf(date)` 按 config 的 `TIME_BLOCKS` 判定；**凌晨 0:00–起床 归昨夜晚块**
- **当前作息边界**（`config.js`，2026-09-13 起）：`wake 08:00 / lunch 12:00 / dinner 18:00 / sleep 24:00`
- 加时自习窗口 = 0:00–7:00（出现在首页三块卡的"加时自习"提示）

### 5.4 数据纪元 `DATA_EPOCH`（清库唯一正确姿势）

`store.js:23` 写死 `const DATA_EPOCH = 3`（**故意不读 config**——设备缓存旧 config 会让判定失效）。

- 判定：本地 `kaoyan:data_epoch` < `DATA_EPOCH` → 清空本地 `time_records` / `active_timer` / **`study_sessions`**
- **★ 必须连 `study_sessions` 一起清**：`migrateOldSessions()` 会在 time_records 为空时把旧表迁回来（曾致 139 条记录"复活"）
- **★ 绝不清 `tasks`**（任务/计划必须保留）
- 清库纪律：先推代码 → 等部署 → 清云端 4 表 → 设备重开；**清库前确认设备已加载新纪元版本**

### 5.5 计划导入体系（三个计划，模式一致）

| 计划 | source | 日期绑定 | 数据文件 | 导入函数（`tasks.js`） |
|---|---|---|---|---|
| 西综计划 | `xizong_plan` | 按日期 | `xizong-plan.js` | `autoImportXizongPlan` |
| 生理滚动复习 | `physio_rolling` | **日期解耦**（`date=""`，不进日历） | `xizong-physio.js` | `autoImportPhysioPlan` |
| 英语单词突围 | `english_words` | 按日期（DAY N = 2026-09-13 + N−1） | `word-plan.js` | `autoImportWordPlan` |

**双重守卫模式（新计划务必照做）**：
1. **先查 Store**：`Store.getTasks().some(t => t.source === "<新source>")` → 有则跳过（跨设备最安全）
2. **再查标记**：`localStorage.getItem("<新flag>")`（标记**不带 `kaoyan:` 前缀**，这样 `pullOnce` 不会清掉它）
3. 调用点：`init()` 本地渲染前 + `pullOnce()` 后**各一次**

**单词计划数据规则**（`word-plan.js`，与用户 APP 截图逐条核对过）：
- 起点 `2026-09-13 = DAY 1` → DAY N = 起点 + (N−1) 天
- 新词日：DAY 1–22 每天 **216** 词；DAY 23–41 每天 **215** 词
- 复习日：**每 4 天**一次（DAY 4/8/…/40），词量 = 前 3 个新词日之和（DAY4=648、DAY24=647、DAY28/32/36/40=645）
- 总 41 天（到 2026-10-23）；**新词日累计恰好 6682 词 = 词书总量**（这是规则正确性的独立佐证）
- 任务标题格式（用户指定）：`09-15 DAY 3 每日单词任务：背单词·新词 216`（由 `vocabTitle(p)` 生成）
- **标题迁移**：`migrateVocabTitles()` 幂等改写旧标题，**只改 title，保留 done/total_focus_sec/time_record_ids/completed_note**（已导入的设备靠它更新，不重新导入）

### 5.6 计时器 `timer.js`

- 双模式：`countup`（打点，正计时）/ `countdown`；`elapsed_sec` + `segments`（暂停分段）
- **停止 <2 秒不落盘**（防误触，`stop()` 内 `el > 2` 判断）——不是 bug
- **停止后总是弹标签抽屉**（试用期用户要求），抽屉内可改**起止时间**（会重算 duration 并清 segments）
- **进入状态模式**：`focus=1` → 播音乐 + 倒计时（时长=音乐时长）→ 转正计时
- **放音乐**：运行面板按钮，正/倒计时都显示，只操作音乐**绝不触碰 active_timer**；文案随状态切换（放音乐/继续/暂停）
- **休息收心预警**：休息倒计时（`kind=break|rest`）剩 2 分钟响铃+震动+通知
- **`window.Timer` API**（供任务页/通话页跨页调用）：`bind / getState / pause / startTask / stopAndMarkDone / stopSilent / getLinkedTaskId`
  - `stopSilent()` = 静默落盘（不弹抽屉、不标完成），v1.12.0 为通话联动新增
- URL 参数：`?up=1&cat=&sub=&tags=&label=`（一键正计时）、`?focus=1`（进入状态）、`?preset=study:45`（快速芯片）
- **无头模式**：页面无 `#timerDisplay` 时只保留后台计时循环 + API（tasks/call 页依赖）

### 5.7 通话边界 `call.js`（规则更新最频繁的模块）

**当前规则（2026-09-15 新规，用户确认）**
1. 长通话**每周 4 次**额度（`call-data.js:93` `maxPerWeek: 4`）；**不再规定单数日/偶数日**
2. 所有通话**只发生在垃圾时间 / 不影响正常进度的时间**
3. 规则部**仅"建议"单数日接听**（`adviseOddDay: true`，非硬规则）
4. **大自习板块**理论不允许通话，但**只提醒**，由人工判断；**人工确认在专注学习 → 坚决不允许接通**

**`judgeToday()` 三档判定**：允许（绿·须双闹钟）/ 需人工判断（琥珀）/ 拒绝（红，含睡眠时段与额度用尽）

**接通流程（`btnCall`，六步）**：① 睡眠时段硬性拒绝（无豁免出口）→ ② 大自习板块人工判断（"是否在专注学习"确认，是则返回）→ ③ 偶数日仅建议确认 → ④ 自身事务勾选检查 → ⑤ 周额度检查 → ⑥ **主站计时联动**（若主站在计时，弹「"X"已 Y 分钟，接通将结束该计时」→ 确认后 `Timer.stopSilent()`）

**规则部豁免（两类，均 30 分钟自动恢复）**
| 类型 | 触发 | 语义 |
|---|---|---|
| `quota` 垃圾时间通话 | 正常申报（理由必填） | 消耗周额度 |
| `violation` 专注时段通话 | 学习区间内的"违规级"申报 | **二次确认 + 专用标签「违规级豁免」+ 备注留痕**（复盘可见） |

- 状态存于 `localStorage:kaoyan:call_override`（含 `until/reason/type/at`），日报备次数存 `kaoyan:call_override_log`
- 判定联动：豁免生效时判定卡显示琥珀色「已报规则部 · 特殊处理中」

**其他**：双闹钟（15min 预警 / 25min 终极，`startDualAlarm`）；今日通话分钟（`todayCallMin()` 从账本累计）+ 「补记通话」按钮（写入账本、计入周额度）；上行联动写 `time_records`（`source="call_boundary"`、`sub_category="linyuchen"`、label「通话」）

### 5.8 PWA 与缓存

- `sw.js` 缓存策略分层：**HTML/JS/CSS = network-first**（部署后立即生效）；其他资源 = stale-while-revalidate
- **改任何静态资源 → 必须升 `CACHE`**（当前 `kaoyan-v118`）；**加新文件 → 同时加进 `SHELL` 数组**
- 7 个页面顶部有**版本自检脚本**：`meta app-version` 与 localStorage `kaoyan:app_version` 不一致 → 清空所有 SW 缓存 + 刷新
- 安卓安装需 **PNG 图标**（192 + 512 + maskable）——只声明 SVG 会导致"装不上"（v1.7.1 已修）

---

## 6. 硬规则清单（违反会出事）

1. **保持纯静态零构建**。加库前问：用户日后能直接改吗？离线能用吗？
2. **时间判定只走 `clock.js` / `blocks.js`**（北京时间），业务代码不得直接 `new Date().getHours()` 判块/判日。
3. **读写数据只走 `window.Store` API**，不直读 localStorage。
4. **统计只走 `TodayRecords`**，不要在业务页另写一套口径。
5. **给表加字段 → 同步改 `sql/schema.sql` 补列**（幂等 `add column if not exists`），并**提醒用户到 Supabase 执行一次**；否则该字段不跨设备同步（见 7.2/7.3）。
6. **版本三重升级（缺一不可）**：
   - `static/js/config.js` → `APP_VERSION`
   - `sw.js` → `CACHE`（+ 新文件进 `SHELL`）
   - **7 个 HTML** 第 7 行 `meta app-version`
7. **新图标**优先复用 `assets/icons/`；缺失会静默留白（控制台 `[icon]` 警告）。
8. **文件操作只用 Read/Write/Edit 工具，绝不用 PowerShell `Set-Content`**（曾致 5 个 HTML 中文乱码 + BOM）。用 `sed -i` 批量改后**必须检查中文完好与无 BOM**。
9. **跨零点逻辑用绝对时间**（Date 差值），不用"当天秒数"。
10. **改动后必做验证**：`node --check 文件` → 本地 8099 硬刷 → 关键断言（见第 9 节）。
11. **新增页面必须进部署清单**——`deploy-pages.yml` 用 `cp *.html` 通配（曾漏复制致线上 404），放根目录即自动。
12. **JSX 文件**（刷题站）校验：`neike-tiku/node_modules/.bin/esbuild src/App.jsx --loader:.jsx=jsx --outfile=/dev/null`。
13. **动手前先 `git status`**：本项目可能被多个 AI 会话/用户同时编辑，发现未提交的陌生改动要先弄清来源再合并（见 7.15）。

---

## 7. 踩坑史：已修 bug 与根因（★ 别重犯）

| # | 症状 | 根因 | 修法 |
|---|---|---|---|
| 7.1 | 偶发「刚记的记录丢失」 | 全量拉取（pullOnce/轮询）返回时覆盖了**本地尚未推送成功**的写入；慢网络下请求"在飞"数秒 | `store.js` 加**脏窗口守卫**：业务写打脏标记 → 推送/删行成功才清；拉取**前**+`await` 返回**落地前**双重检查（`:613-617`、`:922-927`） |
| 7.2 | 任务长期不同步（云端 0 条） | 云端 `tasks` 表**缺 `ref_id`/`source` 列** → PostgREST 整条拒绝；而 **supabase-js 的 upsert 不抛异常、只返回 `{error}`**，旧代码没检查 → **静默失败** | ① `upsertRows` 显式检查 error + **识别缺列报错自动剥离该列重试**；② `schema.sql` 补列（`:853`、`:862`） |
| 7.3 | `day_label` 等字段不跨设备 | `tasks` 表缺 `day_label`/`completed_note`/`note` 列 | `schema.sql` 幂等补列（**需用户到 Supabase 执行一次**） |
| 7.4 | 首页三块统计超 24 小时 | 曾用 `isSameDay(started)&&isSameDay(ended)` 过滤，漏跨天记录 | 提取 `today-records.js` 统一口径（交集裁剪 + 并集合并） |
| 7.5 | 移除某段代码后功能失效（"点了没反应"） | **修改后未 `node --check`**（如 `let` 重复声明、嵌套双引号） | 每次改完立刻 `node --check`；本会话两次被它拦住 |
| 7.6 | 日历里新类型任务**不显示** | `renderCalendar` 的 `typeOrder` **硬编码** `["course","review","problem","other"]`，新 `task_type` 被跳过 | 新增类型必须同时改 `TYPE_META` + `typeOrder` + `typeLabels` |
| 7.7 | 倒计时标签芯片显示异常 | `renderCountdownTagPicker` 漏 `escapeHtml`（正计时侧早前修过） | 两侧对齐（`:272`） |
| 7.8 | CSV 导出后错行 | 备注/标签含逗号、引号、换行未转义 | 升级标准 CSV 转义（整字段加引号 + 内部引号翻倍，`stats.js:165`） |
| 7.9 | 云端删记录后又被"复活" | 本地删了但没删云端行，10s 轮询拉回 | `pushDeleteRow`（本地删完同步删云端） |
| 7.10 | 数据纪元后 139 条记录复活 | `migrateOldSessions()` 在 `time_records` 为空时把旧 `study_sessions` 迁回 | 纪元清库必须**连 `study_sessions` 一起清** |
| 7.11 | 安卓"装不上"PWA | manifest 只声明 SVG 图标（安卓安装判定要求 **PNG 192/512**） | 补 PNG + maskable（v1.7.1） |
| 7.12 | 首页时间轴 hover 闪烁 / 计时器页标签丢失 | 见 git 历史 `a9db7f3`、`ec0186a` | 已修，勿回退 |
| 7.13 | 通话页「睡眠时段」显示绿色却写着"直接拒接" | 旧判定只有"学习/非学习"二元，未区分睡眠 | 新规三档判定 + 四态语义（v1.12.0） |
| 7.14 | 测试时"功能失效"，代码却没问题 | **浏览器缓存了旧 JS**（本地 python http.server + SW） | 测前清 `CacheStorage` + 注销 SW，或用 `fetch(url,{cache:"reload"})`（见 9.2） |
| 7.15 | 编辑报「File has been modified since read」 | 同一文件被**另一个会话/用户并发修改** | 先 `git status`/`git diff` 看清改动来源，**保留对方的机制、只改自己那一处**，别整体覆盖 |
| 7.16 | 版本升了但线上没变 | 只改了 `config.js` 或只改了 HTML，漏了 `sw.js` CACHE | 严格执行第 6 节规则 6 的三重升级 |

---

## 8. 版本升级与部署

### 8.1 每次改动的最小闭环

```bash
# 1) 改代码（Edit/Write 工具）
# 2) 语法检查（必做）
node --check static/js/<改动文件>.js
# 3) 三重升级版本号
#    config.js APP_VERSION / sw.js CACHE（+SHELL）/ 7 个 HTML meta
# 4) 本地冒烟：http://127.0.0.1:8099/ 硬刷（Ctrl+Shift+R）
# 5) 断言测试（见第 9 节）
# 6) 提交 & 推送
git add <明确列出文件>   # 不要无脑 git add -A（.zcode/ 等不该提交）
git commit -m "feat(模块): 中文描述（vX.Y.Z）"
if git push origin main; then echo PUSH_OK; else echo PUSH_FAILED; fi
# 7) 等 2–3 分钟 → curl 验证线上
sleep 150
curl -s https://littlebook888.github.io/kaoyan-site/<页面>.html | grep -o 'app-version" content="[^"]*"'
curl -s https://littlebook888.github.io/kaoyan-site/sw.js | grep -m1 "const CACHE"
```

### 8.2 推送注意事项

- **推送需要梯子**：`git push` 走本机代理（`http://127.0.0.1:7897`，见 `git config --get http.proxy`）；梯子未开时连接被重置
- **判断成败必须看退出码**：`if git push; then ... else ... fi`。**不要用 `git push | tail`**（管道会吞掉失败）
- 推送偶发很慢（本会话有时要 5–8 分钟）→ 可 `run_in_background` 后轮询；**同时用 `git rev-list --count origin/main..HEAD` 或 `git status -sb`（ahead N）确认是否真的推上去了**，不要只看命令是否跑完

### 8.3 Actions 部署卡死

出现在多个运行抢 `github-pages` 环境并发锁时。解法：
- 用已存 Git 凭据调 API 触发 `workflow_dispatch`（新运行会 cancel 旧排队）
- 或让用户在网页上取消积压运行后手动 Run workflow

### 8.4 数据端操作（Supabase）

- 项目：URL/anon key 在 `static/js/config.js`（**用传统 anon key `eyJhbGci...`，不要用 `sb_publishable_`**，否则 DELETE 会被限制）
- 建表/补列：把 `sql/schema.sql` 整段粘到 **SQL Editor** 执行（幂等，可重复跑）
- **已完成/待执行的补列**：`tasks.ref_id`、`tasks.source`、`tasks.day_label`、`tasks.completed_note`、`tasks.note`（若用户尚未执行过 schema.sql，这些字段不会跨设备同步）
- 只读自省（排查缺列/表结构）：`curl "$SB/rest/v1/<表>?select=<列>&limit=1"`（会返回 `42703 column ... does not exist` 或 `PGRST204 Could not find the 'x' column`）

---

## 9. 测试方法论：Node 桩环境法（★ 本项目最有价值的可复用技能）

### 9.1 为什么需要

本项目**没有测试框架**（零构建），且浏览器自动化工具在本环境时有时无。但业务脚本（`tasks.js`/`call.js`/`clock.js`/`home-schedule.js`/`store.js`）都是 IIFE + 依赖 `window`/`document`，**可以在 Node 里用最小的 DOM/Store 桩直接跑真实文件**，对渲染产物做字符串断言。本会话用它拦下了多个真实缺陷。

### 9.2 标准模板（复制即用）

```js
// verify-xxx.js —— 桩环境跑真实业务脚本
const fs = require("fs"), path = require("path"), vm = require("vm");
const ROOT = __dirname;

// —— 1) 造最小 DOM ——
const els = {}; let captured = "";
const mk = (id) => (els[id] = {
  _html: "", id, style: {}, dataset: {}, className: "", textContent: "", value: "", checked: true,
  set innerHTML(v) { this._html = v; }, get innerHTML() { return this._html; },
  classList: { add(){}, remove(){}, contains(){return false}, toggle(){} },
  addEventListener(ev, fn) { (this._h = this._h || {})[ev] = fn; },   // 捕获点击处理器
  setAttribute(){}, getAttribute(){return null}, querySelectorAll(){return[]},
  querySelector(){return null}, appendChild(){}, closest(){return null},
  childElementCount: 0,
});

// —— 2) 造 sandbox ——
const store = new Map();              // 假 localStorage
const sandbox = {
  console, Math, Date, JSON, Object, Array, String, Number, isFinite, parseFloat, parseInt, Promise,
  setInterval(){return 1}, clearInterval(){}, setTimeout(){}, clearTimeout(){},
  navigator: { userAgent: "Mozilla/5.0 (Windows NT 10.0; Win64; x64)" },
};
sandbox.window = sandbox;
sandbox.localStorage = {
  getItem: k => store.has(k) ? store.get(k) : null,
  setItem: (k,v) => store.set(k, String(v)),
  removeItem: k => store.delete(k),
};
const domReady = [];
sandbox.document = {
  readyState: "complete",
  getElementById: id => els[id] || null,
  addEventListener: (ev, fn) => { if (ev === "DOMContentLoaded") domReady.push(fn); },
  querySelectorAll: () => [], querySelector: () => null,
  createElement: () => mk("tmp"), body: { appendChild(){}, removeChild(){} },
};
sandbox.prompt = () => "测试理由"; sandbox.confirm = () => true;   // 需要时可控
vm.createContext(sandbox);

// —— 3) 加载真实文件（顺序 = 页面的脚本顺序）——
const load = r => vm.runInContext(fs.readFileSync(path.join(ROOT, r), "utf8"), sandbox, { filename: r });
load("static/js/config.js");
load("static/js/blocks.js");
sandbox.Blocks.beijing = () => new Date("2026-09-15T15:00:00+08:00"); // 固定"现在"→确定性断言
sandbox.Store = { getTasks: () => [], addTask(t){}, updateTask(){}, subscribeTasks(){},
                  getTimeRecords: () => [], setLocal(){}, getLocal:(k,f)=>f, isCloud:()=>false, setLog(){} };
sandbox.UI = { showAlert(){}, beep(){}, buzz(){}, notify(){}, isMuted:()=>false, isLibrary:()=>false };
sandbox.Icon = { inject(){} };
load("static/js/schedule-data.js");
load("static/js/home-schedule.js");
domReady.forEach(fn => { try { fn(); } catch (e) { console.error("INIT_ERR", e.message); } });

// —— 4) 断言 ——
const html = els.homeSchedule?._html || "";
console.assert(html.includes("晚上自习"), "应显示当前时段");
```

### 9.3 关键技巧

| 技巧 | 说明 |
|---|---|
| **固定"现在"** | 覆盖 `Blocks.beijing` / `Blocks.secOfDay` 返回固定时刻 → 断言可复现（否则结果随真实时间变） |
| **捕获点击处理器** | `addEventListener` 里存 `this._h[ev]`，测试时直接 `els.btnX._h.click()` 触发 |
| **断言用 `_html` 字符串** | 渲染产物是 innerHTML 字符串 → 用 `includes`/正则断言文案与结构 |
| **测试期望写错也要修** | 本会话多次是"代码对、期望错"（如把 legacy 模式总数当成 41）。**先怀疑测试，再怀疑代码**，但必须查清哪边错 |
| **测试 localStorage 原始值** | `store.get(k)` 是字符串，要 `JSON.parse` 才是对象 |
| **`node -e` 的 argv** | `node -e SCRIPT a b c` → `process.argv = [node, a, b, c]`（从 `[1]` 开始） |
| **临时脚本放 `.zcode/`** | 该目录未跟踪，用完即删，不污染仓库 |
| **浏览器自动化可用时** | 优先真机验证视觉；**测前必做**：注销 SW + 清 `caches.keys()` + `fetch(url,{cache:"reload"})` 拉取要测的 js（否则吃旧缓存，见 7.14） |

---

## 10. 【待开发】每日复盘功能（方案已批准，目标版本 v1.13.0）

> 用户已确认：**嵌入统计页**（不新建页面）；**采用下列 8 项指标**。以下方案已批准，可直接实施。

### 10.1 需求

在**统计页内**新增「每日复盘」板块：可选**任意日期**查看该日完整复盘（8 项指标 + 24h 时间轴 + 明细），并支持**一键把该日复盘全量复制到剪贴板**（结构化文本，专为丢给 AI 分析设计）。
日期边界采用**凌晨 4 点**：04:00 前算前一天，04:00 后算当天（天然容纳加时学习）。

### 10.2 4AM 业务日口径（先立地基）

新增到 `blocks.js`（时区无关，遵循"时间判定只走 blocks.js"硬规则）：
```js
const BIZ_START_HOUR = 4;
// 某时间戳属于哪个业务日（北京时间 <4 点 → 归前一天）
function bizDateStr(ts) { ... }
// 该业务日的绝对毫秒窗口 [D 04:00, D+1 04:00)
function bizDayWindow(dateStr) { ... }
```
**实现要点**：北京 04:00 的绝对时间戳 = `Date.UTC(y, m-1, d, 4) - 8*3600*1000`（**不依赖设备时区**，多端一致）。
（注：`today-records.js` 现用本地 `setHours` 划日，与 Blocks 的北京口径有既有分歧；新功能一律使用 Blocks 的北京时间实现，**不要**去改动既有页面口径。）

### 10.3 统计口径泛化（`today-records.js`，现有调用方零改动）

- 抽出 `getRecordsInWindow(startMs, endMs)`：核心（复用现有 id 去重 / segments 优先 / 跨度纠偏 / 8h 安全阀 / 重叠并集合并）
- `getTodayRecords()` 改为薄包装，窗口 = 今日 00:00–24:00（**行为必须与现在完全一致**，home.js/stats.js 不动）
- 新增 `getBizDayRecords(dateStr)`：窗口 = 业务日 04:00–次日 04:00（北京时间）
- ⚠️ 修正既有隐患：进行中记录的兜底 `now` 改为 `Math.min(Date.now(), endMs)`——否则回看历史业务日会把"现在还在跑"的记录算进过去

### 10.4 口径复用（避免第二套算法）

把 `home.js` 的纯函数 **`buildLoveTimeSlots(records, now)` 原样抽取**到新文件 `static/js/day-view.js`（`window.DayView`），`home.js` 改为调用它（**函数体逐字搬迁、调用点替换，不改逻辑**）。
理由：这是「时隙 + 未记录 gap」的唯一算法，复用它才能保证复盘页与首页说的"同一段时间"口径一致（本项目已因口径分叉踩过坑——`today-records.js` 就是为此抽出来的）。

### 10.5 复盘板块内容

**位置**：插在「学霸指数」之后、热力图之前（核心数据 → 学霸指数 → **每日复盘** → 热力图 → 分类明细 → …），并标注顶部的数字是「今日」、复盘卡是「可选日期」，避免混淆。

**① 日期导航**：◀ 日期 ▶ + 「今天」+ 原生 `type="date"` 跳转；浏览位置记 `kaoyan:review_day`（先例：tasks 的 `kaoyan:cal_day`）；标题写明业务日口径「09-15（04:00 → 次日 04:00）」

**② 八项指标**

| 指标 | 口径 |
|---|---|
| 有效学习时长 vs 目标 | 仅 `countTowardGoal` 类；对 `DAILY_GOAL_HOURS`（8h）显示进度条 |
| 早/午/晚三块达成 | 按开始时间归块，各块对 `BLOCK_GOAL_HOURS` 三根进度条 |
| 分类占比明细 | 该业务日分类时长 + 占比（复用 `renderCatBreakdown` 的渲染风格，改传该日数据） |
| 24h 时间轴（04→04） | 复用 `DayView` 时隙 → 紧致时间线（**含未记录时段灰段**，这是给 AI 分析的关键信息） |
| 最长单次专注 | 该日 study 记录 `duration_sec` 最大者 + 起止时间 |
| 碎片时间占比 | <20min 的记录条数与合计（呼应规则部「≥20min 才算有效时间」），并显示有效条数 |
| 加时学习（0–4 点） | 业务日尾部 04:00 前的学习时长（4AM 窗口天然覆盖） |
| 作息边界 | 该日首条记录开始（起床）与最后一条记录结束（收尾/入睡）+ 是否加时 |

**③ 一键复制给 AI 分析**：按钮 → 生成 Markdown 结构化文本 → 写剪贴板（复用 `call.js` 的 `copyText` 模式：`navigator.clipboard.writeText` 优先 + textarea/`execCommand` 兜底）→ `UI.showAlert` 提示。

文本格式（含全部指标 + 分类表 + 逐条时间线 + 未记录时段）：
```
# 每日复盘 2026-09-15（业务日 04:00 – 次日 04:00）
## 摘要
- 有效学习：5小时12分 / 目标 8小时（65%）
- 记录 14 条 · 总记录 11小时20分 · 碎片 4 条共 52 分（7.6%）
- 最长单次专注：1小时50分（09:12–11:02 学西医综合）
- 三块达成：早 2.5/3h｜午 2/2.5h｜晚 0.7/2.5h
- 加时学习（0–4 点）：1小时10分｜起床 08:05｜收尾 00:38
## 分类明细
| 分类 | 时长 | 占比 |
## 时间线
- 08:05–09:30 学习·学西医综合（1小时25分）[高效,专注]
- （未记录）12:00–12:40（40分）
```

### 10.6 改动文件清单

| 文件 | 改动 |
|---|---|
| `static/js/blocks.js` | + 4AM 业务日工具（`BIZ_START_HOUR`/`bizDateStr`/`bizDayWindow`） |
| `static/js/today-records.js` | 抽 `getRecordsInWindow`，加 `getBizDayRecords`，修 `now` 兜底 |
| `static/js/day-view.js`（新） | 承接 `buildLoveTimeSlots`（自 home.js 原样搬迁） |
| `static/js/home.js` | 仅把调用点替换为 `DayView.buildLoveTimeSlots`（零逻辑改动） |
| `static/js/day-review.js`（新） | 复盘板块：日期导航 / 8 指标 / 时间线 / 复制文本生成 |
| `stats.html` | 引入 day-view.js + day-review.js（放在 stats.js 之前）；插入复盘卡片容器 |
| `static/css/theme.css` | 追加 `.rv-*` 样式（复用既有卡片/进度条语言） |
| `sw.js` | `SHELL` 加两个新 js；`CACHE` → v119 |
| 版本号 | `config.js` APP_VERSION + 7 个 HTML meta → **v1.13.0** |

### 10.7 验收标准

1. `node --check` 全部改动 JS
2. **4AM 边界单测（桩环境）**：构造跨夜记录（睡眠 23:00→次日 07:30、加时学习 00:30–03:00），断言
   - 业务日归属正确（04:00 前归前一天）
   - 同一批记录在「自然日 00:00–24:00」与「业务日 04:00–04:00」下切出的时长不同且**各自守恒**（业务日合计 = 自然日两半之和）
   - **时区无关性**：模拟 `TZ=UTC` 与 `TZ=Asia/Shanghai` 结果一致
3. 复盘指标逐项桩测（有效时长 / 三块 / 最长单次 / 碎片 / 加时 / 首末条）
4. 复制文本快照：字段齐全、时间线含未记录段、可被 Markdown 解析
5. **回归**：首页时间轴/列表在各视图与改动前一致（`buildLoveTimeSlots` 搬迁后逐字段比对输出）；统计页原有卡片不受影响
6. 发版 → 线上 curl 验证版本与两个新 js 均 200

### 10.8 明确不做

- 不新建页面（嵌入统计页），不改部署 workflow
- 不动首页/时钟气泡、不改热力图与既有统计口径
- **不臆造"作息达标/未达标"判定**（只呈现事实时间点）
- 智能分析只做**复制**，不在前端接 AI（保持零依赖、离线可用）

---

## 11. 其他待办与用户决策记录

### 11.1 待办

| 项 | 状态 |
|---|---|
| **每日复盘功能** | 方案已批准，**未实施**（见第 10 节） |
| 副站「#10 视觉合并」 | **方案已给，等用户点头**：① 时段配色对齐主界面（睡眠=深蓝 `#1e40af`，替换自造紫色）② 时段状态四态化（禁止红/提醒黄/可考虑绿/自由灰）解决"睡眠写着『直接拒接』却是绿色"的矛盾 ③ 「当前时段」与「主站学习状态」合并为一张「现在 · 边界状态」卡 + 综合结论 |
| 真实初试日期 | `config.js` `EXAM_DATE` = `2026-12-19`（用户确认为真） |
| git 历史隐私清理 | 工作区已清，**历史提交仍有旧文本**；重写历史属破坏性操作，需用户确认 |
| 时区口径不统一 | `today-records.js` 用本地 `setHours`，`Blocks` 用北京时间（国内使用无感）。**新增代码一律用 Blocks 口径**，不要顺手重构既有页面 |
| 刷题站 scope 级覆盖 | 两端同时作答会丢一端（单用户交替使用无感） |
| schema.sql 执行 | **需用户到 Supabase 跑一次**（补 `ref_id/source/day_label/completed_note/note`） |
| 单词突围一键打开 | **需用户导入一次**本机 `.dev-tools/单词突围-协议注册.reg`（双击→是→重启浏览器），之后网页按钮 `dancitw://` 才能拉起 APP。该文件含个人路径故**不入仓库** |
| 死代码 | `numpad` 引用、`sync_ops.applied`、`_timerPushDirty`（清理零收益，暂不动） |

### 11.2 用户明确做过的决策（勿反复询问）

- 番茄钟**不要**；点块自动开计时**不要**；OneDrive 作同步**不要**
- 时间表 / 规则部计划的**学习区间提示**要保留，但**不要**在页面上写"操作指引"（豁免入口放在页面最底部、红色小按钮、点击跳出「选择窗口」）
- 通话借口的**职业人设要保留并强化**（见 1.3）
- 单词任务标题格式：`09-15 DAY 3 每日单词任务：背单词·新词 216`
- 单词计划 41 天（到 2026-10-23），英语六级 **2026-12-12**
- 任务页**不要**内嵌计时 iframe（已移除）
- 休息计时：已加收心预警；**其余休息相关优化暂不动工**
- 「该干嘛就干嘛」→ 已改为「一切的一切都要落地个人成长」；「规则部时间表 · 9.13 起」→ 已改为「马上执行，不要犹豫！」

---

## 12. 用户偏好与协作方式

- **语言与风格**：简体中文；结构化（表格/清单）；接受直白反馈；喜欢「做了什么 + 改动文件 + 如何预览」的简洁总结
- **视觉**：苹果风卡片 + 洛天依蓝（主站）；单词突围相关元素用暖调纸感色板（主色 `#c96442`，参考用户指定的站点配色）
- **协作节奏**：习惯边预览边提修改意见；本地预览服务 `127.0.0.1:8099` 常开
- **测试纪律**：测试本地页面前先清 Cookie/localStorage/sessionStorage，避免残留数据污染判断
- **偏好**：先给方案再动手；不确定的点要**先问**（用户明确说过"如有问题请先询问我，不要瞎开工"）
- **一次性改动要成批交付**：用户常一次列 5–13 条改动，建议**按依赖关系分批提交**（如"主站批"与"副站批"分开），每批独立可上线

---

## 附：文档更新约定

改动本文件时：
1. 同步更新顶部**版本基线**与日期
2. 新增机制写入第 5 节；新增坑写入第 7 节；新增待办写入第 11 节
3. **继续保持零个人信息**（学校/学号/真实姓名/本机用户名/个人路径）
