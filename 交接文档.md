# 考研个人网站管理系统 · 交接文档

> **给接手开发的 AI / 开发者看。** 请先完整读本件，再动手改任何东西。
> 版本基线：**v1.6.0**（2026-09-13）｜ 深度技术细节见 `.trae-cn` 记忆库的 `project_memory.md`（19 节）
> ⚠️ 本件随仓库公开（GitHub），**已清理全部个人信息**；后续增改请勿写入学校/学号/真实姓名等可识别信息。

---

## 一、项目是什么

- **定位**：考研学生的个人时间管理系统（纯静态 HTML/CSS/JS，零框架零构建零依赖）
- **线上主站**：https://littlebook888.github.io/kaoyan-site/
- **子站（刷题站）**：同域下 `/neike-306-tiku/`（5 科题库，React + Vite 构建产物）
- **后端**：Supabase（免费版，PostgreSQL + Realtime），单用户 `kaoyan_user_default`
- **三端**：手机/电脑/平板，PWA 可安装，跨设备实时同步
- **本地预览**：`python -m http.server 8099 --bind 127.0.0.1` → `http://127.0.0.1:8099/index.html`
  （不能 `file://` 直开，否则 fetch/SW/BroadcastChannel 全坏）

### 页面清单

| 页面 | 职责 |
|---|---|
| `index.html` | 首页：实时时钟 / 考研倒计时 / 今日三块 / 今日活动构成（饼图·时钟·时间轴·列表）/ 快速计时 / 链接大全 |
| `timer.html` | 计时器：正计时（打点）/ 倒计时 / 进入状态模式 / 标签抽屉 / 调试控制台 |
| `tasks.html` | 任务：日历·列表·方格·课程表四视图 + 人可研梦生理滚动 |
| `stats.html` | 统计：核心数据 / 学霸指数 / 热力图 / 分类明细 / CSV 导出 |
| `call.html` | 通话边界管控（今日判定 / 话术 / 借口库 / 双闹钟 / 铁律横幅） |
| `reminders.html` | 提醒栏目：晨启宣言 + 6 场景 18 卡 + 规则部速查 |
| `schedule.html` | 时间表：北京时间联动 + 理论 vs 实际对比 + 规则速查 + 心态工具 |

---

## 二、⚠️ 隐私与账号纪律（最重要，务必遵守）

### 账号矩阵

| 账号 | 用途 |
|---|---|
| **littlebook888**（主号） | **本网站**的 GitHub 账号 |
| **xiaobenzic888**（小号） | 打印站专用（无关本项目，**不要混用**） |

### 硬性纪律

1. **提交作者必须是隐私邮箱**：`littlebook888@users.noreply.github.com`（**不要**出现 QQ 邮箱）
2. **网站文案不得出现个人信息**：学校、专业、年级、学号、身份证、手机号
3. **通话边界功能中的对方姓名**：`call.html` / `call.js` / `config.js` 中的显示名是第三方真实姓名，
   如需改为通用称呼（如「通话」），改**显示 label** 即可，数据键 `linyuchen` 不要动（历史记录依赖它）
4. **README / 需求文档**：不要写学校、学号等可识别信息（已于 2026-09-13 清理）

### ⚠️ 搜索引擎收录风险（2026-09-13 实测）

- 站点**无 robots.txt、无 noindex** → 搜索引擎可自由抓取收录
- 当前实测：**尚未被收录**（学号/站点均搜不到），但**收录只是时间问题**
- **Git 历史仍保留旧内容**：清理工作区文件不会删除历史提交里的旧文本。
  若需彻底清除，必须重写历史（`git filter-repo`）+ 强制推送，**属破坏性操作，需用户确认后执行**

---

## 三、三套系统边界（不可混淆）

| 系统 | 职责 | 数据流向 |
|---|---|---|
| ① **时间记录系统**（主线） | `time_records` 账本，对标爱时间/时间日志 | 所有计时结果写入这里 |
| ② **计时器系统** | 正计时（打点）/ 倒计时工具 | **写入** ① |
| ③ **三块展示系统** | 早/午/晚三卡展示 + 电池进度条 | **只读** ①（不写其他系统） |

---

## 四、文件地图（改这里）

| 文件 | 职责 |
|---|---|
| `static/js/config.js` | ⭐ 配置中心：版本号 / Supabase / 作息边界 / 分类体系 / 主题色 / 常用标签 |
| `static/js/store.js` | ⭐ 数据层：localStorage + BroadcastChannel + Supabase 三层同步；**顶部 `DATA_EPOCH` 数据纪元** |
| `static/js/today-records.js` | ⭐ 「今日记录」统一口径（home/stats 共用，改口径只改此处） |
| `static/js/blocks.js` | 大块判定（blockOf / currentKey / remainingSeconds / blockDurationSec） |
| `static/js/clock.js` | 实时北京时间时钟（全站顶部 #liveClock） |
| `static/js/home.js` | 首页：三块 / 饼图 / 时间轴 / 时钟 / 列表 / 记录编辑抽屉 / 补记 |
| `static/js/timer.js` | 计时器：数字输入 / 正倒切换 / 标签抽屉 / 进入状态模式 / 标签回传 |
| `static/js/tasks.js` | 任务四视图 / 西综计划导入（v2 语义分段） |
| `static/js/stats.js` | 统计 / 热力图 / CSV 导出 |
| `static/js/call.js` | 通话边界逻辑 |
| `static/js/reminders-data.js` + `reminders.js` | 提醒栏目（数据 + 渲染） |
| `static/js/schedule-data.js` + `schedule.js` | 时间表（数据 + 渲染） |
| `static/js/home-schedule.js` | 首页「现在该做什么」卡片：时间表联动 + 一键正计时（`timer.html?up=1` 入口在 `timer.js`） |
| `static/css/theme.css` | 全部样式 |
| `sw.js` | PWA 离线缓存（**改静态资源必须升 CACHE 版本号**） |
| `sql/schema.sql` | Supabase 建表/补列（幂等，可重复执行） |
| `neike-tiku/` | 刷题站源码（React + Vite，构建产物发布到 `/neike-306-tiku/`） |

**脚本加载顺序**（各页一致，勿乱）：
`config → blocks → clock → store → today-records → (Supabase CDN) → icon → reveal → ui → 各页业务 js`

---

## 五、核心机制（改代码前必须理解）

### 5.1 同步引擎（store.js）

- `active_timer` **单行表** = 全局唯一「正在跑的计时器」；`time_records` = 历史流水
- 写入链路：`setActiveTimer` → 防抖 500ms（STOP 不防抖）→ 乐观写本地 → BroadcastChannel + Supabase Broadcast（<50ms）→ upsert 持久化
- **version 单调递增**做冲突检测，`op_id` 幂等去重
- **防回环四件套**：心跳只写本地不推远端 / 远端空 15 秒宽容期 / `_stopGuardUntil` 停止保护窗 10 秒 / `_stoppedByRemote` 墓碑
- 实时三通道：Broadcast（主）+ Postgres CDC + 轮询（兜底）

### 5.2 省流轮询（v1.2.2 起）

- 数据轮询（10 秒）：只做**行数探测**（约 100B），行数没变不拉全量；每 5 分钟强制全量兜底
- **页面切后台暂停一切轮询**（零流量），切回前台立即补拉
- 背景：免费版 egress 5GB/月，原「每 10 秒全量拉表」一周见底

### 5.3 数据纪元（全新起点用）

- `store.js` 顶部 `const DATA_EPOCH = 3;`（**写死，不读 config**——设备缓存旧 config 会让判定失效）
- 判定：本地 `kaoyan:data_epoch` < DATA_EPOCH → 清空本地 `time_records` / `active_timer` / `study_sessions`
- **★ 必须连 `study_sessions` 一起清**（`migrateOldSessions()` 会在 time_records 为空时把它迁回，曾致 139 条记录"复活"）
- **★ 绝不清 `tasks`**（任务/计划必须保留）
- 清库纪律：先推代码 → 等部署 → 清云端 4 表 → 设备重开；**清库前确认设备已加载新纪元版本**

### 5.4 统计口径（最易出 bug）

所有统计必须走 `window.TodayRecords.getTodayRecords()`，内含：
id 去重 → 今日交集裁剪（跨天记录只计今日部分）→ 真实时长（优先 segments 暂停分段，否则跨度纠偏 >60s）→ 单条 8h 安全阀 → 重叠区间并集合并

**绝不能**用 `isSameDay(started) && isSameDay(ended)` 过滤（会漏跨天记录，曾致统计超 24 小时）

---

## 六、术语表（用户口令 → 系统字段）

| 用户口令 | 含义 | 当前值 | 权威改动点 |
|---|---|---|---|
| 「夜间学习结束」 | 晚块终点 / 加时自习起点 | **24:00** | `config.js` → `TIME_BLOCKS.sleep` |
| 「午块终点」 | 晚餐时间 | 18:00 | `config.js` → `TIME_BLOCKS.dinner` |
| 「清理计时数据」 | 全新起点 | — | `store.js` DATA_EPOCH +1 并清云端 |
| 「新作息表」 | 《规则部考研时间安排计划表》 | 2026-09-13 起 | `config.js` TIME_BLOCKS + `schedule-data.js` |

**当前作息边界**（2026-09-13 起按规则部计划表）：
`wake 08:00 / lunch 12:00 / dinner 18:00 / sleep 24:00`
（加时自习窗口自动为 0:00–7:00，对应 0:30 上床、7:30 起床）

---

## 七、开发硬规则（防踩坑）

1. **保持纯静态零构建**。加库前问：用户日后能直接改吗？离线能用吗？
2. **时间判定只走 `clock.js` / `blocks.js`**（北京时间），业务代码不得直接 `new Date().getHours()` 判块
3. **读写数据只走 `window.Store` API**，不直读 localStorage
4. **给 `time_records` 加字段** → 同步改 `sql/schema.sql` 补列
5. **新增/改静态资源** → 升 `sw.js` 的 `CACHE` 版本号，否则用户刷新不生效
6. **版本号三重升级**：`config.js` APP_VERSION + `sw.js` CACHE + **7 个 HTML** 的 meta（新增页面记得一起改）
7. **新图标**：优先复用 `assets/icons/`；缺失会静默留白（控制台 `[icon]` 警告）
8. **文件操作只用 Edit/Write**，**绝不用 PowerShell `Set-Content`**（曾致 5 个 HTML 中文乱码 + BOM）
9. **跨零点逻辑用绝对时间**（Date 差值），不用"当天秒数"
10. **改动后验证**：`node --check 文件` + 本地 8099 硬刷（`Ctrl+Shift+R`）
11. **新增页面必须加进部署清单**——`.github/workflows/deploy-pages.yml` 用 `cp *.html` 通配（曾漏复制致线上 404）
12. **JSX 文件**（刷题站）：用 `neike-tiku/node_modules/.bin/esbuild src/App.jsx --loader:.jsx=jsx --outfile=/dev/null` 校验

---

## 八、部署流程

```
改代码 → node --check → 本地 8099 冒烟 → 三重升版本号
  → git add/commit → git push origin main → GitHub Actions 自动部署（约 2 分钟）
  → curl 验证线上 200
```

- **工作流**：`.github/workflows/deploy-pages.yml`（主站 + 刷题站一起构建发布）
- **⚠️ 推送需要梯子**：`git push` 走 127.0.0.1 代理，梯子未开时连接被重置。
  **判断推送成败必须看退出码**（`if git push; then`），不要用 `git push | tail`（管道会吞掉失败）
- **⚠️ Actions 可能排队卡死**：出现在同一个 `github-pages` 环境抢并发锁时。
  解法：用已存 Git 凭据调 API 触发 `workflow_dispatch`（新运行会 cancel 旧排队），
  或让用户在网页上取消积压运行后手动 Run workflow

---

## 九、已知遗留 / 待办

| 项 | 状态 |
|---|---|
| 真实初试日期 | `EXAM_DATE` 仍为占位 `2026-12-19`（用户未确认） |
| Git 历史隐私清理 | 已清理工作区，**历史提交仍有旧文本**，需用户确认是否重写历史 |
| 通话对方姓名显示 | 线上 `call.html` 仍显示第三方真实姓名（待用户决定是否改通用称呼） |
| 时区口径 | blocks.js 按北京时间，时间轴/时钟按设备本地（国内使用无感） |
| 刷题站 scope 级覆盖 | 两端同时作答会丢一端（单用户交替使用无感） |
| 死代码 | `sync_ops.applied`、`_timerPushDirty`（清理零收益，暂不动） |
| 云端 tasks | 当前 0 条（设备本地有数据会自动推回；**任务不能清**） |

---

## 十、用户偏好

- 简体中文，结构化（表格/清单），接受直白反馈
- 苹果风卡片视觉；总结偏好「做了什么 + 改动文件 + 如何预览」的简洁文风
- 习惯边预览边提修改意见；本地预览服务 `127.0.0.1:8099` 常开
- **测试纪律**：测试本地页面前先清 Cookie/localStorage/sessionStorage，避免残留数据污染判断
