# 考研个人网站管理系统 · 交接文档（v3）

> **给接手的 AI / 开发者（含 ChatGPT）**：请**完整读完本件**再动任何代码。
> **版本基线：v1.12.1（2026-09-15）**｜本文档为**唯一权威交接件**（v1.5.1 版内容已全部并入；历史版本见 git）
> 本件除「怎么改」之外，还沉淀了**技术路线、选型理由、实现模式、开发心法**——这些比代码本身更重要。
> ⚠️ 本件随仓库公开（GitHub Pages），**已清理全部个人信息**；后续增改请勿写入学校/学号/真实姓名/本机用户名/个人路径。

---

## 目录

| 节 | 内容 | 什么时候看 |
|---|---|---|
| 0 | 30 秒速览 + **第一天怎么干** | 接手第一眼 |
| 1 | **隐私与账号纪律** | ⭐ 动手前必看 |
| 2 | 项目是什么（定位/边界/页面/加载顺序） | 建立全局观 |
| 3 | **技术路线与选型决策（含"为什么不"）** | ⭐ 想知道为什么这么设计 |
| 4 | **架构与数据流（图）** | ⭐ 理解数据怎么流动 |
| 5 | 文件地图（逐文件职责） | 找文件 |
| 6 | 核心机制（同步/统计/时间/纪元/导入/计时/通话/PWA） | ⭐ 改代码前必读 |
| 7 | **实现方案：可复用代码模式（10 个）** | ⭐ 写新功能照着套 |
| 8 | 硬规则清单（13 条） | ⭐ 违反会出事 |
| 9 | 踩坑史：已修 bug 与根因（16 条） | 别重犯 |
| 10 | **开发经验沉淀（心法 13 条）** | ⭐ 少走弯路 |
| 11 | 版本升级与部署 | 每次交付 |
| 12 | **测试方法论：Node 桩环境法** | ⭐ 本项目唯一测试手段 |
| 13 | **常见问题排查手册（FAQ）** | 出事时查 |
| 14 | **术语表（用户口令 → 系统字段）** | 听懂用户说什么 |
| 15 | **关键锚点速查（改哪里去哪个文件）** | 快速定位 |
| 16 | **版本演进时间线** | 了解来路 |
| 17 | 【待开发】每日复盘 v1.13.0 完整方案 | 下一个功能 |
| 18 | 其他待办 + 用户决策记录 | 别重复问 |
| 19 | 用户偏好与协作方式 | 怎么配合 |
| 附 | 文档更新约定 | 改本文时 |

---

## 0. 30 秒速览 + 第一天怎么干

**这是什么**：一个考研学生的个人时间管理系统。纯静态 HTML/CSS/JS（**零框架、零构建、零依赖**），Supabase 做跨设备同步，手机/电脑/平板三端 + PWA。

**线上**：主站 `https://littlebook888.github.io/kaoyan-site/`｜刷题站同域 `/neike-306-tiku/`
**规模**：7 页 · 24 个 JS（约 9,800 行）· 1 个 CSS（约 2,700 行）· 数据库 7 张表 · 95 次提交

### 第一天怎么干（照着做）

```bash
# 1) 读文档（本件第 1、6、8、10 节是重点）
# 2) 起来看一眼（必须用 HTTP 服务，不能 file:// 直开）
cd "项目根目录" && python -m http.server 8099 --bind 127.0.0.1
#    → http://127.0.0.1:8099/index.html  逐页点一遍（7 页）
# 3) 确认工作区干净 + 看有无他人未提交改动
git status --short && git log --oneline -5
# 4) 确认代理可用（推送需要）
git config --get http.proxy
# 5) 挑一个最小改动走完整闭环（第 11 节），熟悉"改→验→升版本→推→线上核验"
```

> **改动前必问自己三句**：这属于哪一层（页面/业务/口径/数据）？会不会动到别的系统？要不要升版本号？

---

## 1. ⚠️ 隐私与账号纪律（最高优先级）

### 1.1 账号矩阵

| 账号 | 用途 |
|---|---|
| **littlebook888**（主号） | **本网站**的 GitHub 账号 |
| **xiaobenzic888**（小号） | 打印站专用（**与本项目无关，不要混用**） |

### 1.2 绝不能写进仓库的内容

学校、学号、身份证、手机号、专业年级 · 第三方**真实姓名** · 本机 **Windows 用户名 / 个人绝对路径**（如 `C:\Users\<用户名>\...`）
提交作者必须是隐私邮箱 `littlebook888@users.noreply.github.com`（**不要**出现 QQ 邮箱）。

### 1.3 已脱敏 / 有意保留（★ 别"好心"改回去）

| 项 | 状态 | 说明 |
|---|---|---|
| 通话对象显示名 | **已脱敏**（v1.9.0） | 全站统一显示「通话」「通话对象」；**数据键 `linyuchen` 必须保留**（历史记录靠它关联，改键会断数据） |
| 通话借口的**职业人设** | **有意保留，勿脱敏** | 借口里的「值班/加班/急诊/病历/交班/手术/夜班」是**用户明确要求保留并强化**的——现实中确为医学实习、常加班处理急诊，这类"职业义务型"理由最不易被对方纠缠。`static/js/call-data.js` 文件头有同样提示 |
| 一句话记忆 | — | 姓名是**别人的**隐私（要脱敏）；职业人设是**用户自己的**挡箭牌（用户决定保留） |

### 1.4 搜索引擎收录风险（2026-09-13 实测）

- 站点**无 robots.txt / 无 noindex** → 可被自由抓取；当时实测未被收录，但**收录只是时间问题**
- **git 历史仍保留旧文本**（清理工作区不影响历史提交）。彻底清除须重写历史（`git filter-repo` + 强制推送）——**破坏性操作，必须用户确认后执行**

---

## 2. 项目是什么

### 2.1 定位与心智模型

面向考研学生的个人时间管理系统。核心心智模型是**「按三餐切分的大块学习」**（早块/午块/晚块）+ **时间账本**（对标爱时间/时间日志）+ **任务打卡**（对标番茄ToDo）。用户习惯边用边改、按需微调规则。

### 2.2 三套系统边界（不可混淆）

| 系统 | 职责 | 数据流向 |
|---|---|---|
| ① **时间记录系统**（主线） | `time_records` 账本，唯一真相来源 | 所有计时结果写入这里 |
| ② **计时器系统** | 正计时（打点）/ 倒计时工具 | **写入** ① |
| ③ **三块展示系统** | 早/午/晚三卡 + 进度条 | **只读** ①（不得改写非 meal 类数据） |

**现有独立任务块**（互不影响，靠 `source` 隔离）：
| 计划 | source | 日期绑定 | 数据文件 |
|---|---|---|---|
| 西综计划 | `xizong_plan` | 按日期 | `xizong-plan.js` |
| 生理滚动复习 | `physio_rolling` | **日期解耦**（`date=""`，不进日历） | `xizong-physio.js` |
| 英语单词突围 | `english_words` | 按日期（41 天） | `word-plan.js` |

### 2.3 页面清单

| 页面 | 职责 |
|---|---|
| `index.html` | 实时时钟 / 考研倒计时 / **现在该做什么（规则部时间表）** / 今日三块 / 今日活动构成（饼图·时钟·时间轴·列表）/ 快速计时 / 链接大全 |
| `timer.html` | 正计（打点）/ 倒计 / 进入状态模式 / 标签抽屉（可改起止时间）/ 放音乐 / 休息收心预警 / 调试控制台 |
| `tasks.html` | 日历·列表·方格·课程表四视图 + 单词突围独立卡 + 生理滚动复习独立卡 |
| `stats.html` | 核心数据 / 学霸指数 / 热力图 / 分类明细 / 任务报表 / CSV 导出 |
| `call.html` | 当前时段 / 今日判定 / 主站状态 / 话术 / 借口库 / 双闹钟 / 今日通话分钟 / 规则部豁免选择窗口 |
| `reminders.html` | 晨启宣言 + 6 场景 18 卡 + 规则部速查（原文逐字保真） |
| `schedule.html` | 时间表：北京时间联动 + 理论 vs 实际 + 规则速查 + 心态工具 |

### 2.4 各页脚本加载顺序（**勿乱**）

```
index:     config → blocks → clock → store → today-records → [supabase CDN] → icon → reveal → ui
           → schedule-data → home-schedule → home
timer:     config → blocks → clock → store → [supabase] → icon → reveal → ui → schedule-data → timer
tasks:     config → blocks → clock → store → timer → [supabase] → [xlsx] → icon → reveal → ui
           → schedule-data → xizong-plan → xizong-physio → xizong-live → today-xizong-plan → word-plan → tasks
stats:     config → blocks → clock → store → today-records → [supabase] → icon → reveal → ui → schedule-data → stats
call:      config → blocks → clock → store → [supabase] → icon → reveal → ui → timer → schedule-data → call-data → call
reminders: config → blocks → clock → icon → reveal → ui → reminders-data → reminders
schedule:  config → blocks → clock → store → [supabase] → icon → reveal → ui → schedule-data → schedule
```
> 纯数据文件（`*-data.js` / `*-plan.js`）必须在使用它的业务脚本**之前**加载。
> `reminders.html` 特意不加载 store.js（纯静态栏目）；`timer.js` 在 call 页以**无头模式**运行（只暴露 API，不绑 UI）。

---

## 3. 技术路线与选型决策（含"为什么不"）

> 这些决策**经过实际使用验证**，不要因为"看起来不够现代"就推翻它们。

### 3.1 路线总览

| 维度 | 选择 | 关键理由 |
|---|---|---|
| 形态 | **纯静态站点**（GitHub Pages） | 零成本、零运维、离线可用、任何设备都能开 |
| 构建 | **零构建**（无 npm/webpack/vite） | 用户要能**自己改** `config.js` 并立刻生效；改一个文件推上去 2 分钟生效 |
| 组织 | **经典 `<script>` 顺序加载 + `window.X` 命名空间 + IIFE** | 见 3.2 |
| 数据 | **本地优先 + 云端最终一致**（localStorage 为真相，Supabase 做同步） | 断网可用、响应即时；弱网不至于卡住 |
| 同步 | **多通道**（BroadcastChannel + Supabase Broadcast + Postgres CDC + 轮询兜底） | 单通道都会挂：Broadcast 快但只在同浏览器、CDC 慢但可靠、轮询最笨但兜底 |
| 后端 | **Supabase 免费版**（Postgres + Realtime） | 免费、有实时推送、SQL 自助可控、无需自建服务器 |
| 图标 | **Lucide（MIT）本地化 SVG** | 离线可用、`currentColor` 跟随主题、无运行时 CDN |
| 部署 | **GitHub Actions → Pages** | 推 main 即上线；主站与刷题站一起构建 |

### 3.2 决策记录：为什么不用 X（ADR 风格）

| 被否方案 | 否掉的理由（重要） |
|---|---|
| **前端框架**（React/Vue） | 破坏"用户日后能直接改"——改一个文案要懂构建链；且离线/PWA 更重。**刷题站例外**（交互复杂，用 React+Vite） |
| **ES Modules（`import/export`）** | 早期为兼容性选择经典 script；现状：全局命名空间 + 加载顺序已形成稳定约定，改造收益不抵回归风险。**新代码沿用现有模式** |
| **OneDrive / 坚果云做同步** | 需 OAuth 注册、无实时推送（只能轮询）、冲突合并复杂。**用户已明确否决 OneDrive** |
| **云端优先**（每次操作先写云） | 弱网/离线直接不可用；且写云失败要处理 UI 回滚，复杂度高。**本地优先 + 乐观更新 + 失败回滚**更契合单用户场景 |
| **轮询高频全量拉表** | 免费版 egress 5GB/月，**一周见底**（真实事故）。改为：10s 只做**行数探测**（约 100B），行数变了才拉全量；每 5 分钟强制全量兜底；**页面切后台零流量** |
| **番茄钟** | 用户明确"多半用不上"，**已砍**。取而代之：正计时打点 + 固定时长倒计时芯片 |
| **点块自动开计时** | 用户明确否决（要手动控制） |
| **网页调用系统闹钟/计时器** | Web 无此能力（浏览器安全边界）。已记录为**永久限制**：站内闹钟只在页面活着时可靠；`intent://` 可唤起但无回调、不写账本，**不做** |
| **运行时引 CDN 图标/字体** | 离线与 PWA 场景会空白。一律本地化 |
| **morphdom/murphyjs 等动画库** | 其 dist 是 ESM 构建产物，与零构建冲突；改自写 `reveal.js`（IntersectionObserver + CSS） |
| **把日报丢给 AI 要接 API** | 前端不接 AI（要 key、要网络、非离线）。**改为"一键复制结构化文本"**，用户自己贴给 AI |
| **新页面承载每日复盘** | 用户选择**嵌入统计页**（已有统计语境，少一次跳转） |

### 3.3 演进路线（每步都是"更贴用户真实方式"，而非更复杂）

```
番茄钟+笔记 → 砍番茄钟 → 定制学休时长+快速计时+正倒双模 → 对标爱时间/时间日志
→ 手机风数字键盘 → 三餐切分大块体系 → 实时北京时间时钟 → 独立任务块体系（三计划）
→ 规则部时间表联动（首页「现在该做什么」+ 一键正计时）→ 通话边界规则化（豁免分级）
→ PWA 可安装 → 每日复盘（规划中）
```

---

## 4. 架构与数据流

### 4.1 分层模型（写新功能时对号入座）

```
┌──────────────────────────────────────────────────────────────────┐
│ 页面层   index / timer / tasks / stats / call / reminders / schedule │  只做 DOM 与布局
├──────────────────────────────────────────────────────────────────┤
│ 业务层   home.js timer.js tasks.js stats.js call.js …             │  交互、渲染、事件委托
├──────────────────────────────────────────────────────────────────┤
│ 口径层   Blocks(时间/大块) · TodayRecords(统计唯一真相) ·          │  ⚠️ 口径只允许一份实现
│          DayView(时隙) · ScheduleData(计划表)                     │
├──────────────────────────────────────────────────────────────────┤
│ 数据层   Store（window.Store）——— 唯一出入口                       │  ⭐ 禁止绕过它读写 localStorage
└──────────────────────────────────────────────────────────────────┘
```

### 4.2 三端同步数据流

```
               ┌──────────── 手机 PWA / 电脑浏览器 / 平板 PWA ────────────┐
               │            同一份静态站点（GitHub Pages）               │
               └────────────────────────┬────────────────────────────────┘
                                        ▼
                        ┌───────────────────────────────┐
                        │   业务层 / 口径层（见 4.1）    │
                        └───────────────┬───────────────┘
                                        ▼
                        ┌───────────────────────────────┐
                        │  Store API（唯一出入口）       │
                        └───┬───────────┬───────────┬───┘
                            │           │           │
                    ① localStorage  ② BroadcastChannel  ③ Supabase
                    （离线兜底/持久）  （同浏览器多标签）    （跨设备：Broadcast 操作通道
                                                        + Postgres CDC + 轮询兜底）
```

### 4.3 一次"停止计时"的写入时序（理解乐观更新 + 回滚）

```
用户点「停止」
 └─ Store.setActiveTimer(null)
     ├─ STOP 不防抖 → 直接进串行化队列 _writeChain（Promise 链，同一时刻只跑一个写）
     ├─ 读本地快照 snapshotBefore → nextVersion = baseVersion + 1
     ├─ 乐观写本地（setLocalInternal：localStorage + BroadcastChannel 广播）
     ├─ Supabase Broadcast 发 timer-op（其他设备毫秒级应用，不等 DB）
     ├─ 持久化：写 sync_ops（op_id 幂等）+ upsert active_timer（带 version）
     └─ 失败 → 回滚 snapshotBefore（UI 不会"假装成功"）；成功 → 清脏窗口标记
```

### 4.4 计划数据的"三层分离"（三个计划都这个套路）

```
数据文件（纯数据，用户可改）   word-plan.js / xizong-plan.js / xizong-physio.js
        │
        ▼
导入器（幂等 + 双重守卫）       autoImportWordPlan / autoImportXizongPlan / autoImportPhysioPlan
        │  把数据变成 tasks（带独立 source 标记）
        ▼
渲染器（纯读 tasks）           renderWordPlan / renderCalendar / renderPhysio
```
**好处**：数据可随时改（改完重新导入即可）；导入幂等（多设备不会重复建）；渲染与数据解耦（换渲染不影响账本）。

---

## 5. 文件地图（全部文件 · 逐文件职责）

### 5.1 页面（根目录 7 个）

`index.html` `timer.html` `tasks.html` `stats.html` `call.html` `reminders.html` `schedule.html`
> 每页 `<head>` 第 7 行有 `<meta name="app-version">`；**升级版本号必须 7 页同步改**（页面顶部有版本自检脚本：版本不符会清 SW 缓存并刷新）。

### 5.2 `static/js/`（24 个文件 · 约 9,800 行）

| 文件 | 行数 | 职责 |
|---|---|---|
| `config.js` | 136 | ⭐ 配置中心：版本号 / Supabase / 作息边界 / 分类体系（12 类）/ 主题色 / 常用标签（14 个） |
| `blocks.js` | 97 | ⭐ 大块判定 + 北京时间工具（`beijing`/`secOfDay`/`dateStr`/`blockOf`/`currentKey`/`remainingSeconds`/`blockDurationSec`） |
| `clock.js` | 137 | ⭐ 全站实时北京时间时钟 + 「现在该做什么」计划提示芯片 |
| `store.js` | 1153 | ⭐⭐ **数据层/同步底座**（见 6.1） |
| `today-records.js` | 136 | ⭐⭐ **「今日记录」统一统计口径**（见 6.2） |
| `ui.js` | 126 | 共享 UI：静音/图书馆模式、同步徽标、警报、响铃、震动、通知 |
| `icon.js` | 52 | 本地 SVG 图标注入（`data-icon="name"` → `assets/icons/name.svg`） |
| `reveal.js` | 33 | 滚动入场动画（IntersectionObserver） |
| `home.js` | 1244 | 首页：三块 / 饼图 / 时间轴 / 时钟图 / 列表 / 记录编辑抽屉 / 补记 / 一键进入自习 |
| `home-schedule.js` | 306 | 首页「现在该做什么」：时段 hero + 一键正计时 + 分类提醒 + 规则速查 |
| `timer.js` | 2073 | 计时器核心 + `window.Timer` API（见 6.6） |
| `tasks.js` | 1672 | 任务页：四视图 / 三计划导入 / 单词卡 / 生理卡 / 事件委托（见 6.5） |
| `stats.js` | 315 | 统计页：核心数据 / 学霸指数 / 热力图 / 分类明细 / 任务报表 / 两份 CSV 导出 |
| `call.js` | 894 | 通话边界核心（见 6.7） |
| `call-data.js` | 117 | 通话话术底座（借口 13 条 / 话术池 / 周规则 / 豁免配置） |
| `reminders-data.js` | 231 | 提醒栏目数据（**quote 必须逐字保真**） |
| `reminders.js` | 147 | 提醒栏目渲染（highlight 精确子串匹配） |
| `schedule-data.js` | 76 | ⭐ 规则部时间表数据（9 时段 + 6 规则 + 6 心态工具，**主副站共用**） |
| `schedule.js` | 175 | 时间表页渲染 |
| `word-plan.js` | 56 | ⭐ 单词突围计划数据（41 天） |
| `xizong-plan.js` | 554 | 西综计划数据（61 条日计划） |
| `xizong-physio.js` | 35 | 生理滚动复习数据（43 天，循环生成） |
| `xizong-live.js` | 35 | 官方计划本地镜像（4 天） |
| `today-xizong-plan.js` | 29 | 单日快照（供「从网站获取」） |
| `langqian/langqian.js` | 44 | 浪前「成长角落」占位（已被提醒栏目取代，保留作历史） |

### 5.3 其他

| 路径 | 说明 |
|---|---|
| `static/css/theme.css` | **全部样式**（约 2,700 行）。苹果风玻璃卡片 + 洛天依蓝 |
| `sw.js` | Service Worker 离线缓存（**改静态资源必须升 `CACHE`**，当前 `kaoyan-v118`；新文件加进 `SHELL`） |
| `manifest.webmanifest` | PWA 清单（含 PNG 192/512 + maskable） |
| `sql/schema.sql` | Supabase 建表 + 幂等补列（**可重复执行**） |
| `assets/` | `icon.svg` + PNG 图标 + `icons/`（约 80 个 Lucide SVG）+ `study_start.mp3` |
| `.github/workflows/deploy-pages.yml` | 部署工作流（主站 + 刷题站） |
| `neike-tiku/` | 刷题站源码（React + Vite，产物发布到 `/neike-306-tiku/`） |
| `通话边界管控分站-需求与集成文档.md` | 通话边界**需求基线**（V1.2，规则集与话术库全集） |
| `docs/` | 早期文档（题库复刻计划/评估等） |
| `.dev-tools/` | **本机工具（已 gitignore）**：含协议注册 `.reg` 等 |

---

## 6. 核心机制（改代码前必须理解）

### 6.1 数据层 `store.js`（最复杂的文件）

**表/键**：`active_timer`（**单行表** = 全局唯一"正在跑的计时器"）· `time_records`（历史流水，主线）· `tasks` · `events` · `goals` · `study_sessions`（旧表兼容）

| 机制 | 位置 | 作用 |
|---|---|---|
| `DATA_EPOCH = 3` | `store.js:23` | 数据纪元（见 6.4） |
| `_writeChain` 串行化 | — | 同一时刻只有一个 active_timer 写入 |
| `version` 单调递增 | — | 冲突检测（LWW-Register 标准实现） |
| `op_id` 幂等去重 | — | 重复操作不重复应用（防重放） |
| 防回环四件套 | — | 心跳只写本地 / 远端空 15s 宽容期 / 停止保护窗 10s / `_stoppedByRemote` 墓碑 |
| **脏窗口守卫** | `:613-617`（`LOCAL_DIRTY_SKIP_MS=120s`） | ⭐ 本地未推送成功期间**禁止全量拉取覆盖本地**；拉取前 + `await` 返回落地前**双重检查**（见 9.1） |
| **缺列自愈 upsert** | `:853` `_missingColumnName` / `:862` `upsertRows` | ⭐ 云端缺列时自动剥离该列重试并告警（见 9.2） |
| 数组表删除 | `:933` `pushDeleteRow` | 本地删完必须删云端行，否则 10s 轮询把已删记录拉回来 |
| 旧表迁移 | `:767` `migrateOldSessions` | `study_sessions` → `time_records`（time_records 为空时触发） |
| 省流轮询 | — | 10s 行数探测（约 100B）→ 变了才拉全量；5 分钟强制全量；**切后台零流量** |

**Store 公开 API**（`window.Store` @ `store.js:1137`）
`isCloud / pullOnce / getLocal / setLocal / initSupabase` · `getActiveTimer / setActiveTimer / subscribeActiveTimer / isSyncing / subscribeSyncStatus` · `getTimeRecords / addTimeRecord / updateTimeRecord / deleteTimeRecord / subscribeTimeRecords` · `getTasks / addTask / updateTask / deleteTask / addFocusToTask / subscribeTasks` · `getSessions / addSession` · `getEvents / addEvent` · `getGoals / setGoals` · `setLog`

### 6.2 统计口径 `today-records.js`（唯一真相来源）

**所有"今日"统计必须走 `TodayRecords.getTodayRecords()`**（`:38`；home.js/stats.js 共用）。

内部**五步严格执行**：
1. `id` 去重（防同步/导入重复）
2. 与今日 `[00:00, 24:00)` **求交集**（跨天记录只计今日部分）
3. 真实时长：**优先 segments**（暂停分段求和），否则按跨度纠偏（差值 >60s 视为脏数据）
4. 单条今日时长 **>8h 截断**（防异常值撑爆统计）
5. **重叠区间并集合并**（防重复计时总和 >24h）

**绝不能**用 `isSameDay(started) && isSameDay(ended)` 过滤——会漏跨天记录（曾致统计超 24 小时）。
`segDurSec(raw, winS, winE)`（`:22`）是分段求交核心，可复用。

### 6.3 时间判定 `blocks.js` + `clock.js`

- 全站基准 = **北京时间（UTC+8）**，与设备时区无关（`beijing()` 换算后取值）
- `secOfDay()` = 北京当天已过秒；`dateStr()` = 北京当天 `YYYY-M-D`
- 大块：`blockOf(date)` 按 `TIME_BLOCKS` 判定；**凌晨 0:00–起床 归昨夜晚块**
- **当前作息边界**（2026-09-13 起）：`wake 08:00 / lunch 12:00 / dinner 18:00 / sleep 24:00`
- 加时自习窗口 = 0:00–7:00（首页三块卡的"加时自习"提示）

### 6.4 数据纪元 `DATA_EPOCH`（清库唯一正确姿势）

`store.js:23` 写死 `const DATA_EPOCH = 3`（**故意不读 config**——设备缓存旧 config 会让判定失效）。

- 判定：本地 `kaoyan:data_epoch` < `DATA_EPOCH` → 清空 `time_records` / `active_timer` / **`study_sessions`**
- **★ 必须连 `study_sessions` 一起清**：`migrateOldSessions()` 会在 time_records 为空时把旧表迁回来（曾致 139 条记录"复活"）
- **★ 绝不清 `tasks`**（任务/计划必须保留）
- 清库纪律：先推代码 → 等部署 → 清云端 4 表 → 设备重开；**清库前确认设备已加载新纪元版本**

### 6.5 计划导入体系（三计划同套路）

**双重守卫（新计划务必照做）**：
1. **先查 Store**：`Store.getTasks().some(t => t.source === "<新source>")` → 有则跳过（跨设备最安全）
2. **再查标记**：`localStorage.getItem("<新flag>")`（标记**不带 `kaoyan:` 前缀**，`pullOnce` 不会清掉它）
3. 调用点：`init()` 本地渲染前 + `pullOnce()` 后**各一次**

**单词计划数据规则**（`word-plan.js`，与用户 APP 截图逐条核对过）：
- 起点 `2026-09-13 = DAY 1` → DAY N = 起点 + (N−1) 天；总 41 天（到 2026-10-23）
- 新词日：DAY 1–22 每天 **216** 词；DAY 23–41 每天 **215** 词
- 复习日：**每 4 天**一次（DAY 4/8/…/40），词量 = 前 3 个新词日之和（DAY4=648、DAY24=647、DAY28/32/36/40=645）
- **新词日累计恰好 6682 词 = 词书总量**（规则正确性的独立佐证）
- 标题格式（用户指定）：`09-15 DAY 3 每日单词任务：背单词·新词 216`（`vocabTitle(p)`）
- **标题迁移** `migrateVocabTitles()`：幂等改写旧标题，**只改 title，保留 done/total_focus_sec/time_record_ids/completed_note**

### 6.6 计时器 `timer.js`

- 双模式：`countup`（打点）/ `countdown`；`elapsed_sec` + `segments`（暂停分段）
- **停止 <2 秒不落盘**（防误触 `stop()` 内 `el > 2`）——不是 bug
- **停止后总是弹标签抽屉**（用户要求），抽屉内可改**起止时间**（重算 duration 并清 segments）
- **进入状态模式**：`focus=1` → 播音乐 + 倒计时（时长=音乐时长）→ 转正计时
- **放音乐**：运行面板按钮，正/倒计时都显示，只操作音乐**绝不触碰 active_timer**
- **休息收心预警**：休息倒计时（`kind=break|rest`）剩 2 分钟响铃+震动+通知
- **`window.Timer` API**：`bind / getState / pause / startTask / stopAndMarkDone / stopSilent / getLinkedTaskId`
  （`stopSilent()` = 静默落盘，不弹抽屉不标完成，v1.12.0 为通话联动新增）
- URL 参数：`?up=1&cat=&sub=&tags=&label=`（一键正计时）· `?focus=1`（进入状态）· `?preset=study:45`（快速芯片）
- **无头模式**：页面无 `#timerDisplay` 时只保留后台循环 + API（tasks/call 页依赖）

### 6.7 通话边界 `call.js`（规则更新最频繁）

**当前规则（2026-09-15 新规，用户确认）**
1. 长通话**每周 4 次**额度（`call-data.js:93` `maxPerWeek: 4`）；**不再规定单数日/偶数日**
2. 所有通话**只发生在垃圾时间 / 不影响正常进度的时间**
3. 规则部**仅"建议"单数日接听**（`adviseOddDay: true`，非硬规则）
4. **大自习板块**理论不允许，但**只提醒**，人工判断；**人工确认在专注学习 → 坚决不允许接通**

**`judgeToday()` 三档**：允许（绿·须双闹钟）/ 需人工判断（琥珀）/ 拒绝（红：睡眠时段、额度用尽）

**接通流程（`btnCall` 六步）**：① 睡眠时段硬性拒绝（无豁免出口）→ ② 大自习人工判断（"是否在专注学习"确认，是则返回）→ ③ 偶数日仅建议确认 → ④ 自身事务勾选 → ⑤ 周额度 → ⑥ **主站计时联动**（主站在计时则弹「"X"已 Y 分钟，接通将结束该计时」→ 确认后 `Timer.stopSilent()`）

**规则部豁免（两类，均 30 分钟自动恢复）**
| 类型 | 触发 | 语义 |
|---|---|---|
| `quota` 垃圾时间通话 | 正常申报（理由必填） | 消耗周额度 |
| `violation` 专注时段通话 | 学习区间内"违规级"申报 | **二次确认 + 专用标签「违规级豁免」+ 备注留痕**（复盘可见） |

状态存 `localStorage:kaoyan:call_override`；日报备次数存 `kaoyan:call_override_log`。

**其他**：双闹钟（15min 预警 / 25min 终极）；今日通话分钟（`todayCallMin()` 从账本累计）+「补记通话」按钮；上行联动写 `time_records`（`source="call_boundary"`、`sub_category="linyuchen"`、label「通话」）

### 6.8 PWA 与缓存

- `sw.js` 策略分层：**HTML/JS/CSS = network-first**（部署后立即生效）；其他 = stale-while-revalidate
- **改静态资源 → 必须升 `CACHE`**；**加新文件 → 同时加进 `SHELL` 数组**
- 7 页顶部有**版本自检**：`meta app-version` ≠ localStorage `kaoyan:app_version` → 清空所有 SW 缓存 + 刷新
- 安卓安装需 **PNG 图标**（192 + 512 + maskable）——只声明 SVG 会"装不上"（v1.7.1 已修）

---

## 7. 实现方案：可复用代码模式（10 个）

> 写新功能时**直接照套**，保持全站一致性。

### 模式 1：IIFE + `window.X` 命名空间（标准模块骨架）

```js
/* ===== xxx.js —— 一句话职责
 * 依赖：config.js / blocks.js …；加载顺序须在 … 之后 ===== */
window.Xxx = (function () {
  const C = window.APP_CONFIG;
  let 内部状态 = null;

  function 私有函数() { /* ... */ }

  function init() {
    if (!document.getElementById("xxxRoot")) return;   // ⭐ 页面无此容器 → 静默不初始化
    render();
    setInterval(tick, 1000);
  }

  if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", init);
  else init();

  return { init, render };   // 只暴露必要的
})();
```
**要点**：① 顶部注释写职责+依赖+加载顺序；② `init` 先做容器存在性检查（同一脚本在多页复用时不报错）；③ 只暴露必要 API。

### 模式 2：数据文件 → 导入器 → 渲染器（三层分离）

见 4.4。**要点**：数据文件纯数据（用户可改）；导入器**幂等 + 双重守卫**；渲染器只读 `Store.getTasks()`。

### 模式 3：事件委托 + `data-*` 属性（不要逐元素绑定）

```js
// 容器上挂一次；子元素用 data-动作 声明意图
container.addEventListener("click", (e) => {
  const id = e.target.closest("[data-start]")?.dataset?.start;
  if (!id) return;              // ⭐ 每个分支都要 return
  /* 处理 */
  render();                     // 处理后统一重渲
});
```
**为什么**：渲染函数每次 `innerHTML` 重建 DOM，逐元素绑定会失效且重复绑定；委托只挂一次、永不失效。（`tasks.js` / `call.js` / `home.js` 全用这个模式）

### 模式 4：状态机放 localStorage（豁免窗口范例）

```js
const KEY = "kaoyan:xxx_state";
function getState() {                    // 读时校验过期（自愈）
  try {
    const o = JSON.parse(localStorage.getItem(KEY) || "null");
    if (!o || !o.until || o.until <= Date.now()) return null;   // ⭐ 过期即失效
    return o;
  } catch (e) { return null; }
}
function setState(reason, minutes, type) { /* 写 + 记审计日志 */ }
function clearState() { /* ... */ }
```
**要点**：读时判过期（不依赖定时器清理）；带 `type` 区分语义；附带审计计数便于复盘。

### 模式 5：幂等迁移（改数据而不丢用户进度）

```js
function migrateXxx() {
  const plan = window.XXX_PLAN || [];
  const byKey = {}; plan.forEach(p => byKey[p.key] = p);
  const next = Store.getTasks().map(t => {          // 走全量 map，不改的就原样返回
    if (t.source !== "xxx") return t;
    const p = byKey[keyOf(t)]; if (!p) return t;
    const want = 目标值(p);
    if (t.字段 === want) return t;                  // ⭐ 幂等：相同不写
    return { ...t, 字段: want };                    // ⭐ 只改目标字段，其余保留
  });
  if (changed) Store.setLocal("tasks", next);       // ⭐ 一次批量写（避免 N 次全表推送）
}
```
**要点**：**只改要改的字段**（保留 `done`/`total_focus_sec` 等进度）· 幂等（相同不写）· 批量写一次 · 在导入守卫分支里调用。

### 模式 6：自愈式云端交互（缺列剥离重试）

```js
async function upsertRows(table, rows) {
  let payload = rows.map(r => ({ ...r, user_id: C.USER_ID }));
  for (let i = 0; i < 5; i++) {
    const { error } = await sb.from(table).upsert(payload, { onConflict: "id" });
    if (!error) return true;                        // ⭐ supabase-js 不抛异常，必须查 error
    const col = _missingColumnName(error);           // 解析 PGRST204 / 42703
    if (!col) { console.error(...); return false; }
    console.warn(`云端 ${table} 缺列「${col}」→ 跳过该列继续同步；请补 schema.sql`);
    payload = payload.map(r => { const c = { ...r }; delete c[col]; return c; });
  }
  return false;
}
```
**要点**：① **`supabase-js` 的写操作不抛异常、只返回 `{error}`**——不检查就等于静默失败；② 缺列时剥离重试（让其他字段照常同步）+ 打印可执行提示；③ 失败**不要清脏标记**。

### 模式 7：优雅降级（数据文件缺失不报错）

```js
const D = window.SCHEDULE_DATA;
if (!D || !D.slots) return "";       // 数据未加载 → 静默不渲染，不留白不报错
```
（`clock.js` 的计划提示芯片就靠这个在无计划数据的页面自动隐藏）

### 模式 8：计算字段"纯函数化"（便于复用与测试）

把"由数据算出结果"的逻辑写成**只依赖入参**的纯函数：`buildLoveTimeSlots(records, now)`、`clockChartSVG(records, now, selKey)`、`compute()`。
**收益**：可被多页复用（不再各写一套口径）· 可用第 12 节的桩环境直接测。

### 模式 9：容器显隐统一收敛到 `render()`

**反例（旧代码）**：按钮显隐散落在 4 个地方（start/stop/focus/订阅回调），改一个漏三个。
**正例（现状）**：全部在 `render()` 里根据**当前状态**决定：
```js
if (replayMusicEl) replayMusicEl.style.display = focusMode ? "none" : "block";
```
**要点**：状态 → 渲染是单向的；只要有状态变化就调 `render()`，显隐永远是自洽的。

### 模式 10：URL 参数驱动的页面间流转

页面 A 需要页面 B 执行动作时，用 URL 参数（而非 `postMessage`/`localStorage` 传令）：
`timer.html?up=1&cat=study&sub=&tags=&label=晚上自习`（一键正计时）· `?focus=1`（进入状态）· `?preset=study:45`
**要点**：B 页解析后**立即把动作对齐到 UI（不等防抖回灌）** → 执行 → `history.replaceState` **清参数防刷新重复触发**。

---

## 8. 硬规则清单（违反会出事）

1. **保持纯静态零构建**。加库前问：用户日后能直接改吗？离线能用吗？
2. **时间判定只走 `clock.js` / `blocks.js`**（北京时间），业务代码不得直接 `new Date().getHours()` 判块/判日。
3. **读写数据只走 `window.Store` API**，不直读 localStorage。
4. **统计只走 `TodayRecords`**，不要在业务页另写一套口径。
5. **给表加字段 → 同步改 `sql/schema.sql` 补列**（幂等 `add column if not exists`），并**提醒用户到 Supabase 执行一次**；否则该字段不跨设备同步（见 9.2/9.3）。
6. **版本三重升级（缺一不可）**：`config.js` APP_VERSION · `sw.js` CACHE(+SHELL) · **7 个 HTML** 第 7 行 meta。
7. **新图标**优先复用 `assets/icons/`；缺失会静默留白（控制台 `[icon]` 警告）。
8. **文件只用 Read/Write/Edit 工具，绝不用 PowerShell `Set-Content`**（曾致 5 个 HTML 中文乱码 + BOM）。用 `sed -i` 批量改后**必须检查中文完好与无 BOM**。
9. **跨零点逻辑用绝对时间**（Date 差值），不用"当天秒数"。
10. **改动后必做验证**：`node --check` → 本地 8099 硬刷 → 关键断言（第 12 节）。
11. **新增页面必须进部署清单**——`deploy-pages.yml` 用 `cp *.html` 通配（曾漏复制致线上 404），放根目录即自动。
12. **JSX 文件**（刷题站）校验：`neike-tiku/node_modules/.bin/esbuild src/App.jsx --loader:.jsx=jsx --outfile=/dev/null`。
13. **动手前先 `git status`**：本项目可能被多个会话/用户同时编辑，发现陌生未提交改动要先弄清来源再合并（见 9.15）。

---

## 9. 踩坑史：已修 bug 与根因（★ 别重犯）

| # | 症状 | 根因 | 修法 |
|---|---|---|---|
| 9.1 | 偶发「刚记的记录丢失」 | 全量拉取（pullOnce/轮询）返回时覆盖了**本地尚未推送成功**的写入；慢网络下请求"在飞"数秒 | `store.js` 加**脏窗口守卫**：业务写打脏标记 → 推送/删行成功才清；拉取**前** + `await` 返回**落地前**双重检查（`:613-617`、`:922-927`） |
| 9.2 | 任务长期不同步（云端 0 条） | 云端 `tasks` 表**缺 `ref_id`/`source` 列** → PostgREST 整条拒绝；而 **supabase-js 的 upsert 不抛异常、只返回 `{error}`**，旧代码没检查 → **静默失败** | `upsertRows` 显式检查 error + **缺列自动剥离重试**；`schema.sql` 补列（`:853`、`:862`） |
| 9.3 | `day_label` 等字段不跨设备 | `tasks` 表缺 `day_label`/`completed_note`/`note` 列 | `schema.sql` 幂等补列（**需用户到 Supabase 执行一次**） |
| 9.4 | 首页三块统计超 24 小时 | 曾用 `isSameDay(started)&&isSameDay(ended)` 过滤，漏跨天记录 | 提取 `today-records.js` 统一口径（交集裁剪 + 并集合并） |
| 9.5 | 改动后功能失效（"点了没反应"） | **未 `node --check`**（如 `let` 重复声明、嵌套双引号） | 每次改完立刻 `node --check`；本会话两次被它拦住 |
| 9.6 | 日历里新类型任务**不显示** | `renderCalendar` 的 `typeOrder` **硬编码**，新 `task_type` 被跳过 | 新增类型必须同时改 `TYPE_META` + `typeOrder` + `typeLabels` |
| 9.7 | 倒计时标签芯片异常 | `renderCountdownTagPicker` 漏 `escapeHtml`（正计时侧早前修过） | 两侧对齐（`timer.js:272`） |
| 9.8 | CSV 导出错行 | 备注/标签含逗号、引号、换行未转义 | 标准 CSV 转义（整字段加引号 + 内部引号翻倍，`stats.js:165`） |
| 9.9 | 云端删记录后又被"复活" | 本地删了但没删云端行，10s 轮询拉回 | `pushDeleteRow`（本地删完同步删云端） |
| 9.10 | 数据纪元后 139 条记录复活 | `migrateOldSessions()` 在 `time_records` 为空时把旧 `study_sessions` 迁回 | 纪元清库必须**连 `study_sessions` 一起清** |
| 9.11 | 安卓"装不上"PWA | manifest 只声明 SVG 图标（安卓安装判定要求 **PNG 192/512**） | 补 PNG + maskable（v1.7.1） |
| 9.12 | 首页时间轴 hover 闪烁 / 一键进入自习在手机失效 | 见 git 历史 `a9db7f3`、`ec0186a` | 已修，勿回退 |
| 9.13 | 通话页「睡眠时段」显示绿色却写"直接拒接" | 旧判定只有"学习/非学习"二元，未区分睡眠 | 三档判定 + 四态语义（v1.12.0） |
| 9.14 | 测试时"功能失效"，代码却没问题 | **浏览器缓存了旧 JS**（本地 http.server + SW） | 测前清 `CacheStorage` + 注销 SW，或 `fetch(url,{cache:"reload"})`（见 12.3） |
| 9.15 | 编辑报「File has been modified since read」 | 同一文件被**另一个会话/用户并发修改** | 先 `git status`/`git diff` 看清来源，**保留对方机制、只改自己那处**，别整体覆盖 |
| 9.16 | 版本升了线上没变 | 只改 `config.js` 或只改 HTML，漏了 `sw.js` CACHE | 严格执行硬规则 6 |

---

## 10. 开发经验沉淀（心法 13 条）

> 这一节是全项目最"省时间"的部分——都是真实踩过才总结出来的。

1. **单一真相来源（Single Source of Truth）**
   凡是"同一件事在两处算"的地方，早晚会分叉。判据：**同一口径被 ≥2 个页面使用时，立刻提取公共模块**（`today-records.js`、`DayView` 就是这么来的）。
   *反例教训*：home.js 与 stats.js 各写一份"今日记录"，加暂停分段口径时改了两处，其中一处漏改 → 统计超 80 小时。

2. **先立地基，再盖功能**
   涉及基础口径的功能（如"4AM 业务日"），**顺序必须是**：先在 `blocks.js` 加工具函数 → 写单测（含时区无关性）→ 再让业务用。
   直接把口径逻辑写在业务页里，后面无法复用、无法测试、必然分叉。

3. **幂等一切（Imports/Migrations/Retries）**
   任何"可能会执行两次"的逻辑都必须幂等：导入（查 source/标记）· 迁移（相同值不写）· 重试（`op_id` 去重）· 写云端（upsert 而非 insert）。

4. **外部数据一律当脏数据**
   云端拉回的数据、用户手改的 localStorage、旧版本遗留的记录：**都要兜底**（时间解析 NaN、`duration_sec` 与跨度不符、字段缺失、列不存在）。
   项目里所有关键的"纠偏/截断/兜底"都是这条心法的产物。

5. **迁移优先于重导（保用户数据是底线）**
   要改已有数据的形态时，**永远选"只改字段的迁移"**，绝不"删掉重导"——后者会丢 `done`、`total_focus_sec`、`time_record_ids` 等用户进度。迁移必须保留一切非目标字段。

6. **分层隔离，靠 `source` 划界**
   新计划/新功能**开一个新的 `source` 值**，配独立守卫、独立渲染、独立监听。这样它与既有系统天然互不影响——这也是用户反复强调"互不影响"的落地方式。

7. **限定改动范围，不顺手重构**
   用户常一次提 5–13 条改动。正确做法：**只改被点名的东西**，别"顺便优化"别处。改到自己不熟悉的文件时，先读完相关段落再动笔。
   例外：当发现的问题会**直接导致本次改动失效**（如日历 `typeOrder` 硬编码会吞掉新类型），必须一并修并说明。

8. **小步提交 + 分批上线**
   按依赖关系分批（如"主站批"与"副站批"），每批独立可上线、可回滚。**一次提交只做一类事**，commit message 写清"做了什么 + 为什么"。

9. **改动后立即自检（三连）**
   `node --check` → 关键断言（第 12 节）→ 线上 curl 核验。**不要等"都改完再测"**——错误会叠在一起，定位成本指数上升。

10. **状态 → 渲染 单向流动**
    显隐/文案/禁用状态一律由 `render()` 从当前状态推导（模式 9）。散落的 `style.display = ...` 是 bug 温床。

11. **注释写"为什么"，不写"是什么"**
    项目现有注释大量记录**历史事故原因**（如"曾致 139 条记录复活""曾硬编码 23:40 导致改配置不同步"）。**这些注释是资产，不要删**——它们防止后来者（包括 AI）重犯同一个错。

12. **隐私与功能冲突时，先问用户**
    "姓名要脱敏"和"职业人设是最好用的借口"是**两个独立决策**：前者涉及第三方隐私，后者是用户自己的事。**不要用一个原则去处理两类问题**——问清楚再动。

13. **不臆造标准，不替用户做价值判断**
    例如"作息是否达标"：只呈现**事实**（睡眠起止时间），不生成"达标/不达标"结论——判定标准应由用户在规则部里定义。
    涉及"该不该、好不好"的建议，**给方案 + 说明取舍，等用户点头**（用户明确要求："如有问题请先询问我，不要瞎开工"）。

---

## 11. 版本升级与部署

### 11.1 每次改动的最小闭环

```bash
# 1) 改代码（Read/Write/Edit 工具）
# 2) 语法检查（必做）
node --check static/js/<改动文件>.js
# 3) 三重升级版本号：config.js APP_VERSION / sw.js CACHE(+SHELL) / 7 个 HTML meta
# 4) 本地冒烟：http://127.0.0.1:8099/ 硬刷（Ctrl+Shift+R）
# 5) 断言测试（第 12 节）
# 6) 提交 & 推送（明确列文件，不要无脑 git add -A：.zcode/ 等不该提交）
git add <文件…> && git commit -m "feat(模块): 中文描述（vX.Y.Z）"
if git push origin main; then echo PUSH_OK; else echo PUSH_FAILED; fi
# 7) 等 2–3 分钟 → 线上核验
sleep 150
curl -s https://littlebook888.github.io/kaoyan-site/<页面>.html | grep -o 'app-version" content="[^"]*"'
curl -s https://littlebook888.github.io/kaoyan-site/sw.js | grep -m1 "const CACHE"
curl -s -o /dev/null -w "%{http_code}\n" https://littlebook888.github.io/kaoyan-site/<新增文件>
```

### 11.2 推送注意事项

- **推送需要梯子**：`git push` 走本机代理（`http://127.0.0.1:7897`，查法 `git config --get http.proxy`）；梯子未开连接被重置
- **判断成败必须看退出码**：`if git push; then ... fi`。**不要用 `git push | tail`**（管道会吞掉失败）
- 推送偶发很慢（本会话有时 5–8 分钟）→ 可后台跑再轮询；**用 `git status -sb`（显示 `ahead N`）或 `git rev-parse HEAD` vs `origin/main` 确认真推上去了**，不要只看命令是否跑完

### 11.3 Actions 部署卡死

多个运行抢 `github-pages` 环境并发锁时会排队卡死。解法：用已存 Git 凭据调 API 触发 `workflow_dispatch`（新运行会 cancel 旧排队）；或让用户在网页取消积压后手动 Run workflow。

### 11.4 数据端操作（Supabase）

- URL/anon key 在 `static/js/config.js`（**用传统 anon key `eyJhbGci...`，不要用 `sb_publishable_`**，否则 DELETE 会被限制）
- 建表/补列：把 `sql/schema.sql` 整段粘到 **SQL Editor** 执行（幂等，可重复跑）
- **待执行的补列**：`tasks.ref_id`、`tasks.source`、`tasks.day_label`、`tasks.completed_note`、`tasks.note`（用户若未执行过 schema.sql，这些字段不跨设备同步）
- 只读自省排查：`curl "$SB/rest/v1/<表>?select=<列>&limit=1"` → 返回 `42703 column ... does not exist` 或 `PGRST204 Could not find the 'x' column` 即缺列

---

## 12. 测试方法论：Node 桩环境法（★ 本项目最有价值的可复用技能）

### 12.1 为什么需要

本项目**没有测试框架**（零构建），浏览器自动化工具又时有时无。但业务脚本都是 IIFE + 依赖 `window`/`document`，**可以在 Node 里用最小 DOM/Store 桩直接跑真实文件**，对渲染产物做字符串断言。本会话用它拦下了多个真实缺陷（含两处"代码对、断言写错"的反向纠错）。

### 12.2 标准模板（复制即用）

```js
// .zcode/verify-xxx.js —— 用完即删（该目录未跟踪）
const fs = require("fs"), path = require("path"), vm = require("vm");
const ROOT = path.join(__dirname, "..");

// —— 1) 最小 DOM：每个被脚本引用的 id 都造一个桩 ——
const els = {};
const mk = (id) => (els[id] = {
  _html: "", id, style: {}, dataset: {}, className: "", textContent: "", value: "", checked: true,
  childElementCount: 0,
  set innerHTML(v) { this._html = v; }, get innerHTML() { return this._html; },
  classList: { add(){}, remove(){}, contains(){ return false; }, toggle(){} },
  addEventListener(ev, fn) { (this._h = this._h || {})[ev] = fn; },   // ⭐ 捕获处理器以便测试触发
  setAttribute(){}, getAttribute(){ return null; },
  querySelectorAll(){ return []; }, querySelector(){ return null; },
  appendChild(){}, closest(){ return null; },
});
["taskContainer", "viewSwitch", "……"].forEach(mk);

// —— 2) sandbox（含假 localStorage / navigator / prompt / confirm）——
const store = new Map();
const sandbox = {
  console, Math, Date, JSON, Object, Array, String, Number, isFinite, parseFloat, parseInt, Promise,
  setInterval(){ return 1; }, clearInterval(){}, setTimeout(){}, clearTimeout(){},
  navigator: { userAgent: "Mozilla/5.0 (Windows NT 10.0; Win64; x64)" },
};
sandbox.window = sandbox;
sandbox.localStorage = {
  getItem: k => store.has(k) ? store.get(k) : null,
  setItem: (k, v) => store.set(k, String(v)),
  removeItem: k => store.delete(k),
};
const domReady = [];
sandbox.document = {
  readyState: "complete",
  getElementById: id => els[id] || null,
  addEventListener: (ev, fn) => { if (ev === "DOMContentLoaded") domReady.push(fn); },
  querySelectorAll: () => [], querySelector: () => null,
  createElement: () => mk("tmp"), body: { appendChild(){}, removeChild(){} },
};
sandbox.prompt = () => "测试理由";            // 需要时返回确定值
sandbox.confirm = () => true;                 // 控制确认分支
vm.createContext(sandbox);

// —— 3) 按"页面脚本顺序"加载真实文件 ——
const load = r => vm.runInContext(fs.readFileSync(path.join(ROOT, r), "utf8"), sandbox, { filename: r });
load("static/js/config.js");
load("static/js/blocks.js");
sandbox.Blocks.beijing = () => new Date("2026-09-15T15:00:00+08:00");   // ⭐ 固定"现在"
sandbox.Store = { getTasks: () => [], addTask(t){}, updateTask(){}, subscribeTasks(){},
                  getTimeRecords: () => [], setLocal(){}, getLocal: (k, f) => f,
                  isCloud: () => false, setLog(){} };
sandbox.UI = { showAlert(){}, beep(){}, buzz(){}, notify(){}, isMuted: () => false, isLibrary: () => false };
sandbox.Icon = { inject(){} };
load("static/js/schedule-data.js");
load("static/js/home-schedule.js");
domReady.forEach(fn => { try { fn(); } catch (e) { console.error("INIT_ERR", e.message); } });

// —— 4) 对渲染产物做字符串断言 ——
const html = els.homeSchedule?._html || "";
console.assert(html.includes("晚上自习"), "应显示当前时段");
```

### 12.3 关键技巧

| 技巧 | 说明 |
|---|---|
| **固定"现在"** | 覆盖 `Blocks.beijing` / `Blocks.secOfDay` 返回固定时刻 → 断言可复现（否则随真实时间变） |
| **捕获点击处理器** | `addEventListener` 存进 `this._h[ev]`，测试时 `els.btnX._h.click()` 直接触发 |
| **断言用 `_html` 字符串** | 渲染产物是 innerHTML → 用 `includes` / 正则断言文案与结构 |
| **先怀疑测试，再怀疑代码** | 本会话多次"代码对、期望错"（如把 legacy 模式总数当 41）。**必须查清哪边错**，不要盲改代码 |
| **localStorage 原始值是字符串** | `store.get(k)` 后要 `JSON.parse` 才是对象 |
| **`node -e` 的 argv** | `node -e SCRIPT a b c` → `process.argv = [node, a, b, c]`（**从 `[1]` 开始**） |
| **Python 元组别写反** | `checks.append((条件, 描述))` — 反了会在拼接时报 `TypeError` |
| **临时脚本放 `.zcode/`** | 该目录未跟踪，用完即删，不污染仓库 |
| **浏览器自动化可用时** | 优先真机看视觉。**测前必做**：注销 SW + 清 `caches.keys()` + `fetch(url,{cache:"reload"})` 拉要测的 js（否则吃旧缓存，见 9.14） |
| **对比"改动前后"输出** | 搬迁/重构类改动（如抽 `buildLoveTimeSlots`），用同一份输入跑改动前后两个版本，**逐字段比对**输出一致性 |

---

## 13. 常见问题排查手册（FAQ）

| 症状 | 排查顺序 | 处置 |
|---|---|---|
| 线上没生效 | ① `curl` 看页面 `meta app-version` ② 看 `sw.js` 的 `CACHE` ③ 看 Actions 是否成功 | 漏升三重版本号 → 补升；Actions 卡死 → 见 11.3；用户端 → 硬刷 |
| 推送失败 | `git config --get http.proxy` → 梯子是否开 → 看退出码 | 开代理重推；**不要用管道吞掉退出码** |
| 数据不同步 | 浏览器控制台看 `[store]` 告警（缺列/推送失败）→ 查云端表有无列 → 查 `Store.isCloud()` | 缺列 → 补 `schema.sql`；脏窗口 → 看 9.1；把 `data_epoch` 与 `DATA_EPOCH` 对齐 |
| 记录"丢失" | 看是否 <2 秒的计时（**不落盘是设计**）→ 查 `time_records` 云端有无 → 看是否被脏窗口/拉取覆盖 | 见 9.1；补记用首页"补记"或通话页"补记通话" |
| 统计数据离谱（>24h） | 是否走了 `TodayRecords`（业务页自算就会错）→ 是否有跨天记录 | 见 9.4；统一走口径层 |
| 安卓装不上 PWA | manifest 是否有 **PNG 192/512** → 是否 Chrome/Edge（小米自带浏览器不支持）→ 手机是否有 GMS | 图标补齐（9.11）；无 GMS 只能"添加到主屏幕"（快捷方式，非真安装） |
| 到点没响（闹钟/通知） | 页面是否还活着（Web 限制）→ 是否静音/图书馆模式 → 通知权限 | 属**永久限制**（见 3.2）；可引导用户把浏览器/PWA 设"无限制"后台 |
| 新类型任务在日历不显示 | 是否同时改了 `TYPE_META` + `typeOrder` + `typeLabels` | 见 9.6 |
| 新计划重复导入 | 守卫顺序是否"先查 Store 再查标记" → 标记是否误带 `kaoyan:` 前缀 | 见 6.5 |
| 页面某个区块空白 | 控制台找 `[icon]` 缺图标告警 / JS 报错；数据文件是否加载（加载顺序） | 补图标；调脚本顺序（第 2.4 节） |
| 文件中文乱码/BOM | 是否用了 PowerShell `Set-Content` | 见硬规则 8；用 Edit 工具重写该文件 |
| 本地测试结果诡异 | 是否残留旧 localStorage / 旧 SW 缓存 | 清 Cookie+localStorage+sessionStorage，注销 SW，硬刷 |

---

## 14. 术语表（用户口令 → 系统字段）

| 用户口令 | 含义 | 当前值 / 权威改动点 |
|---|---|---|
| **「夜间学习结束」** | 晚块终点 / 加时自习起点 | **24:00** → `config.js` `TIME_BLOCKS.sleep` |
| **「午块终点」** | 晚餐时间 | 18:00 → `TIME_BLOCKS.dinner` |
| **「清理计时数据」** | 全新起点 | `store.js` `DATA_EPOCH` +1，并清云端 4 表（见 6.4） |
| **「新作息表」** | 《规则部时间安排计划表》 | 2026-09-13 起 → `config.js` `TIME_BLOCKS` + `schedule-data.js` |
| **早块 / 午块 / 晚块** | 按三餐切分的学习大块 | `wake 08:00 / lunch 12:00 / dinner 18:00 / sleep 24:00` |
| **加时自习** | 晚块结束到次日 7:00 的学习 | 自动窗口 0:00–7:00 |
| **有效时间** | 规则部：单段 ≥20min 才算有效 | 仅作提示口径；`<20min` 在复盘里算"碎片" |
| **碎片时间 / 碎片学习** | 实习等场景的短时学习 | 标签【实习碎片时间】【实习大块时间】【普通碎片时间】 |
| **规则部** | 用户自设的个人规则体系 | 相关文案与规则集中在 `schedule-data.js` / `call-data.js` |
| **报规则部 / 豁免** | 学习区间内申请临时通话许可 | `call.js` 的 override（`quota` / `violation` 两类） |
| **垃圾时间** | 不影响正常进度的零碎时间 | 通话规则的时间前提 |
| **DAY N** | 计划的天序号 | 西综 `xizong_plan` / 生理 `physio_rolling` / 单词 `english_words` 各自的 `day_label` |
| **三块电池** | 各块时间流逝进度条 | `home.js` `updateBlockBatteries` |
| **一键进入自习** | 音乐预热 + 自动正计时 | `timer.html?focus=1` |
| **一键吃饭 / 一键睡觉** | 首页按时段预置正计时 | `timer.html?up=1&cat=meal&sub=regular&label=午餐` 等 |

---

## 15. 关键锚点速查（改哪里去哪个文件）

| 要改什么 | 去哪里 |
|---|---|
| 作息边界 / 分类体系 / 常用标签 / 目标时长 / 主题色 / 版本号 | `static/js/config.js` |
| 大块判定 / 北京时间工具 | `static/js/blocks.js:92`（导出） |
| 今日统计口径 | `static/js/today-records.js:38` `getTodayRecords` / `:22` `segDurSec` |
| 同步引擎 / 脏窗口 / 缺列自愈 / 数据纪元 | `static/js/store.js:23`(EPOCH) `:613`(脏窗口) `:862`(upsertRows) `:1137`(API) |
| 计时器行为 / Timer API / 收心预警 | `static/js/timer.js`（`window.Timer` 在文件末尾导出） |
| 任务四视图 / 三计划导入 / 日历分组 | `static/js/tasks.js`（`collectPlanDays` ~118、`renderCalendar` ~137、`autoImport*` ~1160+） |
| 单词计划数据（41 天规则） | `static/js/word-plan.js` |
| 单词任务标题格式 / 标题迁移 | `static/js/tasks.js` 的 `vocabTitle(p)` / `migrateVocabTitles()` |
| 通话判定三档 / 接通流程 / 豁免 | `static/js/call.js`（`judgeToday` / `btnCall` / `setOverride`） |
| 通话话术 / 周额度 / 豁免配置 | `static/js/call-data.js` |
| 规则部时间表（9 时段/规则/心态工具） | `static/js/schedule-data.js`（主副站共用） |
| 提醒栏目文案（逐字保真） | `static/js/reminders-data.js` |
| 全站样式 | `static/css/theme.css` |
| 图标 | `assets/icons/*.svg`（用 `data-icon="名字"` 引用） |
| 数据库建表/补列 | `sql/schema.sql` |
| 离线缓存 / 版本号 | `sw.js`（`CACHE` + `SHELL`） |
| 部署 | `.github/workflows/deploy-pages.yml` |

---

## 16. 版本演进时间线

> 完整明细见 `git log`（95 次提交）。以下是重要节点。

| 版本 | 里程碑 |
|---|---|
| v1.0.x | 上线 + Supabase 跨设备同步 + GitHub Pages 部署 |
| v1.1.0~1.1.3 | 首页时钟/列表对标爱时间（0–23 刻度表盘）· 全盘高危修复（H1-H3/M1-M6）· **统计口径统一到 `getTodayRecords`** · 倒计时按真实时长落盘 |
| v1.2.0~1.2.2 | **多端同步 v2 架构**（操作队列 + Version 冲突检测 + Broadcast 操作广播 + UI 同步锁）· 今日记录口径提取公共模块 · **轮询省流改造（治理 egress）** |
| v1.3.0~1.3.9 | 提醒栏目（状态触发式）· **数据纪元机制** · 三块时间百分比/电池进度条 · 滚动复习语义分段 · 打印站入口 |
| v1.4.0~1.4.2 | 手机端体验修复 · 时间记录 hover 闪烁修复 |
| v1.5.0~1.5.1 | **时间表附属页**（北京时间联动 + 理论 vs 实际）· **作息切到 9.13 新计划表** |
| v1.6.0~1.6.9 | **首页「现在该做什么」+ 一键正计时** · 一键吃饭/睡觉 · 灰度与文案打磨 · **全面体检：脏窗口守卫等 bug 根修** |
| v1.7.0~1.7.3 | 状态行按类别提醒 · 放音乐移到计时器页 · **PWA 图标修复（安卓可安装）** · 计划提示芯片 |
| v1.8.0 | 通话页接入计划表（**学习区间强提示**） |
| v1.9.0~1.9.1 | 偶数日改「建议」· **全面脱敏（姓名）** · 恢复并扩充职业借口库 |
| v1.10.0~1.10.1 | **规则部豁免机制** · 底部红按钮 + 「选择窗口」（浪前思维链） |
| v1.11.0~1.11.5 | **单词突围独立任务块**（41 天）· 配色/标题格式 · 冲刺倒计时 · 去内嵌计时 · 实习分类 + Anki 标签 + 标签链路体检 |
| v1.12.0~1.12.1 | **通话按 9.15 新规重构**（三档判定/豁免分级/主站计时联动/通话分钟）· 休息收心预警 + 三个实习标签 |
| **v1.13.0（规划）** | **每日复盘**（嵌入统计页 + 4AM 业务日 + 一键复制给 AI）→ 见第 17 节 |

---

## 17. 【待开发】每日复盘功能（方案已批准，目标 v1.13.0）

> 用户已确认：**嵌入统计页**（不新建页面）；采用下列 **8 项指标**。方案已批准，可直接实施。

### 17.1 需求

在**统计页内**新增「每日复盘」板块：可选**任意日期**查看该日完整复盘（8 指标 + 24h 时间轴 + 明细），并支持**一键把该日复盘全量复制到剪贴板**（结构化文本，专为丢给 AI 分析设计）。
日期边界采用**凌晨 4 点**：04:00 前算前一天，04:00 后算当天（天然容纳加时学习）。

### 17.2 4AM 业务日口径（先立地基）

新增到 `blocks.js`（时区无关，遵循"时间判定只走 blocks.js"）：
```js
const BIZ_START_HOUR = 4;
function bizDateStr(ts) { /* 北京时间 <4 点 → 归前一天 */ }
function bizDayWindow(dateStr) { /* 该业务日绝对毫秒窗口 [D 04:00, D+1 04:00) */ }
```
**实现要点**：北京 04:00 的绝对时间戳 = `Date.UTC(y, m-1, d, 4) - 8*3600*1000`（**不依赖设备时区**，多端一致）。
（`today-records.js` 现用本地 `setHours` 划日，与 Blocks 的北京口径有既有分歧；新功能一律用 Blocks 实现，**不要**顺手改既有页面口径。）

### 17.3 统计口径泛化（`today-records.js`，现有调用方零改动）

- 抽出 `getRecordsInWindow(startMs, endMs)`：核心（复用 id 去重 / segments 优先 / 跨度纠偏 / 8h 安全阀 / 重叠并集合并）
- `getTodayRecords()` 改为薄包装，窗口 = 今日 00:00–24:00（**行为必须与现在完全一致**，home.js/stats.js 不动）
- 新增 `getBizDayRecords(dateStr)`：窗口 = 业务日 04:00–次日 04:00（北京时间）
- ⚠️ 修正既有隐患：进行中记录的兜底 `now` 改为 `Math.min(Date.now(), endMs)`——否则回看历史业务日会把"现在还在跑"的记录算进过去

### 17.4 口径复用（避免第二套算法）

把 `home.js` 的纯函数 **`buildLoveTimeSlots(records, now)` 原样抽取**到新文件 `static/js/day-view.js`（`window.DayView`），`home.js` 改为调用它（**函数体逐字搬迁、调用点替换，不改逻辑**，用 12.3 的"改动前后输出比对"验证）。
理由：这是「时隙 + 未记录 gap」的唯一算法，复用才能保证复盘页与首页说的"同一段时间"口径一致（本项目已因口径分叉踩过坑，见心法 1）。

### 17.5 复盘板块内容

**位置**：插在「学霸指数」之后、热力图之前；并标注顶部数字是「今日」、复盘卡是「可选日期」，避免混淆。

**① 日期导航**：◀ 日期 ▶ + 「今天」+ 原生 `type="date"` 跳转；浏览位置记 `kaoyan:review_day`（先例：tasks 的 `kaoyan:cal_day`）；标题写明业务日口径「09-15（04:00 → 次日 04:00）」

**② 八项指标**

| 指标 | 口径 |
|---|---|
| 有效学习时长 vs 目标 | 仅 `countTowardGoal` 类；对 `DAILY_GOAL_HOURS`（8h）显示进度条 |
| 早/午/晚三块达成 | 按开始时间归块，各块对 `BLOCK_GOAL_HOURS` 三根进度条 |
| 分类占比明细 | 该业务日分类时长 + 占比（复用 `renderCatBreakdown` 渲染风格，改传该日数据） |
| 24h 时间轴（04→04） | 复用 `DayView` 时隙 → 紧致时间线（**含未记录时段灰段**，这是给 AI 分析的关键信息） |
| 最长单次专注 | 该日 study 记录 `duration_sec` 最大者 + 起止时间 |
| 碎片时间占比 | `<20min` 的记录条数与合计（呼应规则部「≥20min 才算有效时间」），并显示有效条数 |
| 加时学习（0–4 点） | 业务日尾部 04:00 前的学习时长（4AM 窗口天然覆盖） |
| 作息边界 | 该日首条记录开始（起床）与最后一条记录结束（收尾/入睡）+ 是否加时 |

**③ 一键复制给 AI 分析**：按钮 → 生成 Markdown 结构化文本 → 写剪贴板（复用 `call.js` 的 `copyText` 模式：`navigator.clipboard.writeText` 优先 + textarea/`execCommand` 兜底）→ `UI.showAlert` 提示。

文本格式（含全部指标 + 分类表 + 逐条时间线 + 未记录时段）：
```
# 每日复盘 2026-09-15（业务日 04:00 – 次日 04:00）
## 摘要
- 有效学习：5小时12分 / 目标 8小时（65%）
- 记录 14 条 · 总记录 11小时20分 · 碎片 4 条共 52 分（7.6%）
- 最长单次专注：1小时50分（09:12–11:02 学西医综合）
- 三块达成：早 2.5/3h｜午 2/2.5h｜晚 0.7/2.5h
- 加时学习（0–4 点）：1小时10分｜起床 08:05｜收尾 00:38
## 分类明细
| 分类 | 时长 | 占比 |
## 时间线
- 08:05–09:30 学习·学西医综合（1小时25分）[高效,专注]
- （未记录）12:00–12:40（40分）
```

### 17.6 改动文件清单

| 文件 | 改动 |
|---|---|
| `static/js/blocks.js` | + 4AM 业务日工具（`BIZ_START_HOUR`/`bizDateStr`/`bizDayWindow`） |
| `static/js/today-records.js` | 抽 `getRecordsInWindow`，加 `getBizDayRecords`，修 `now` 兜底 |
| `static/js/day-view.js`（新） | 承接 `buildLoveTimeSlots`（自 home.js 原样搬迁） |
| `static/js/home.js` | 仅把调用点替换为 `DayView.buildLoveTimeSlots`（零逻辑改动） |
| `static/js/day-review.js`（新） | 复盘板块：日期导航 / 8 指标 / 时间线 / 复制文本生成 |
| `stats.html` | 引入 day-view.js + day-review.js（放在 stats.js 之前）；插入复盘卡片容器 |
| `static/css/theme.css` | 追加 `.rv-*` 样式（复用既有卡片/进度条语言） |
| `sw.js` | `SHELL` 加两个新 js；`CACHE` → v119 |
| 版本号 | `config.js` APP_VERSION + 7 个 HTML meta → **v1.13.0** |

### 17.7 验收标准

1. `node --check` 全部改动 JS
2. **4AM 边界单测（桩环境）**：构造跨夜记录（睡眠 23:00→次日 07:30、加时学习 00:30–03:00），断言
   - 业务日归属正确（04:00 前归前一天）
   - 同一批记录在「自然日 00:00–24:00」与「业务日 04:00–04:00」下切出的时长不同且**各自守恒**（业务日合计 = 自然日两半之和）
   - **时区无关性**：模拟 `TZ=UTC` 与 `TZ=Asia/Shanghai` 结果一致
3. 复盘指标逐项桩测（有效时长 / 三块 / 最长单次 / 碎片 / 加时 / 首末条）
4. 复制文本快照：字段齐全、时间线含未记录段、可被 Markdown 解析
5. **回归**：首页时间轴/列表在各视图与改动前一致（`buildLoveTimeSlots` 搬迁后逐字段比对）；统计页原有卡片不受影响
6. 发版 → 线上 curl 验证版本与两个新 js 均 200

### 17.8 明确不做

- 不新建页面（嵌入统计页），不改部署 workflow
- 不动首页/时钟气泡、不改热力图与既有统计口径
- **不臆造"作息达标/未达标"判定**（只呈现事实时间点）
- 智能分析只做**复制**，不在前端接 AI（保持零依赖、离线可用）

---

## 18. 其他待办与用户决策记录

### 18.1 待办

| 项 | 状态 |
|---|---|
| **每日复盘功能** | 方案已批准，**未实施**（见第 17 节） |
| 副站「视觉合并」 | **方案已给，等用户点头**：① 时段配色对齐主界面（睡眠=深蓝 `#1e40af`，替换自造紫色）② 时段状态四态化（禁止红/提醒黄/可考虑绿/自由灰），解决"睡眠写着『直接拒接』却是绿色"的矛盾 ③ 「当前时段」与「主站学习状态」合并为一张「现在 · 边界状态」卡 + 综合结论 |
| git 历史隐私清理 | 工作区已清，**历史提交仍有旧文本**；重写历史属破坏性操作，需用户确认 |
| 时区口径不统一 | `today-records.js` 用本地 `setHours`，`Blocks` 用北京时间（国内使用无感）。**新增代码一律用 Blocks 口径**，不要顺手重构既有页面 |
| 刷题站 scope 级覆盖 | 两端同时作答会丢一端（单用户交替使用无感） |
| schema.sql 执行 | **需用户到 Supabase 跑一次**（补 `ref_id/source/day_label/completed_note/note`） |
| 单词突围一键打开 | **需用户导入一次**本机 `.dev-tools/单词突围-协议注册.reg`（双击→是→重启浏览器），之后网页按钮 `dancitw://` 才能拉起 APP。该文件含个人路径故**不入仓库** |
| 死代码 | `numpad` 引用、`sync_ops.applied`、`_timerPushDirty`（清理零收益，暂不动） |

### 18.2 用户已明确做过的决策（勿反复询问）

- 番茄钟**不要**；点块自动开计时**不要**；OneDrive 作同步**不要**
- 学习区间提示要保留，但**不要**在页面写"操作指引"（豁免入口放页面最底部、红色小按钮、点击跳出「选择窗口」）
- 通话借口的**职业人设要保留并强化**（见 1.3）
- 单词任务标题格式：`09-15 DAY 3 每日单词任务：背单词·新词 216`
- 单词计划 41 天（到 2026-10-23）；英语六级 **2026-12-12**
- 任务页**不要**内嵌计时 iframe（已移除）
- 休息计时：已加收心预警；**其余休息相关优化暂不动工**
- 「该干嘛就干嘛」→「一切的一切都要落地个人成长」；「规则部时间表 · 9.13 起」→「马上执行，不要犹豫！」

---

## 19. 用户偏好与协作方式

- **语言与风格**：简体中文；结构化（表格/清单）；接受直白反馈；喜欢「做了什么 + 改动文件 + 如何预览」的简洁总结
- **视觉**：苹果风卡片 + 洛天依蓝（主站）；单词突围相关元素用暖调纸感色板（主色 `#c96442`，参考用户指定站点）
- **协作节奏**：习惯边预览边提修改意见；本地预览服务 `127.0.0.1:8099` 常开
- **测试纪律**：测试本地页面前先清 Cookie/localStorage/sessionStorage
- **偏好**：**先给方案再动手**；不确定的点**先问**（用户原话："如有问题请先询问我，不要瞎开工"）
- **成批交付**：用户常一次列 5–13 条改动 → **按依赖关系分批提交**（如"主站批"与"副站批"分开），每批独立可上线

---

## 附：文档更新约定

改动本文件时：
1. 同步更新顶部**版本基线**与日期
2. 新机制 → 第 6 节；新实现模式 → 第 7 节；新心法 → 第 10 节；新坑 → 第 9 节；新待办 → 第 18 节；新术语 → 第 14 节
3. **继续保持零个人信息**（学校/学号/真实姓名/本机用户名/个人路径）
