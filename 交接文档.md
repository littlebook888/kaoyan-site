# 考研个人网站 · 交接文档（2026-08-26 · 最新需求基线）

> 给**下一位接手编辑的 AI** 看。请先完整读这份，再读 `README.md` 补架构底子。
> 本件记录的是**当前真实需求和最新实现状态**（README 是较早的维护版，部分信息已过时，以本件为准）。

---

## 0. 阅读顺序（30 秒版）

1. 本件第 1~2 节：是什么 + 最新需求基线
2. 本件第 5 节：给接手 AI 的开发指南（**必读，防踩坑**）
3. `README.md` 第 5（同步层）、6（数据模型）节补架构细节
4. 动手：改动集中在 `static/js/config.js`、`home.js`、`timer.js`、`blocks.js`、`timer.html`、`theme.css`

---

## 1. 项目是什么

用户（福建医科大学 放射医学 大四，考研备战，主跟「天天师兄」306 西综，2026-12 初试）的个人考研网站。
**纯静态 HTML/CSS/JS，零框架零构建零依赖**，苹果风卡片 + 洛天依蓝（#66ccff）视觉，三端（手机/电脑/平板）可 PWA 装 App。

**三套系统，边界不可乱**：
- ① **时间记录系统（主线）** = `time_records` 账本，对标爱时间/时间日志
- ② **计时器系统** = 正计时（打点）/倒计时工具，**写入**时间记录
- ③ **三块展示系统（早/午/晚）** = 只展示 +「开始吃饭」按钮（写入 meal 类记录）

本地预览：`python -m http.server 8099 --bind 127.0.0.1` → http://127.0.0.1:8099/index.html
（不能 `file://` 直开，否则 fetch/SW/BroadcastChannel 全坏。）

---

## 2. 最新需求基线（用户当前想要的，务必保持）

### 2.1 首页 · 今日三块（`home.js` renderBlocks）
- 早/午/晚三张卡片：时间窗、已学时长、块状态。
- **剩余时间**：`本块剩 x小时x分钟`，数字部分**黑色加粗**（棕色标签字体说明：`本块剩 <b>…</b>`）。
- **没有"目标/进度"**：卡片底部显示 `本块区间共 x小时x分钟`（即该块总时长），**不做单块时段目标百分比**。
- **加时自习**：晚块结束（`23:40`）后到次日 `07:00`，晚块卡片进入加时自习状态，显示
  `加时自习：距离早上7:00还剩 x小时x分钟`，并点亮「现在」+ 高亮。
  ⚠️ 必须用**绝对时间**判断（当天 23:40 → 次日 07:00），**跨零点不能断**。已在 `home.js` 实现。

### 2.2 时间块边界（`config.js` TIME_BLOCKS，用户已敲定）
| 边界 | 值 | 含义 |
|---|---|---|
| wake（早块起） | **08:00** | 早块从这里算起 |
| lunch（早块末/午块起） | 12:00 | |
| dinner（午块末/晚块起） | 17:30 | |
| sleep（晚块末） | **23:40** | 之后进入加时自习 |

### 2.3 分类体系（升级：一级 + 二级，`config.js` TIME_CATEGORIES）
| 一级 | 二级 |
|---|---|
| 学习 🟩 | 学西医综合 / 学英语 / 学政治 / 学习其他 |
| 工作 🔵 | 实习 / 副业 |
| 吃饭 🟠 | 吃零食 |
| 家务 🟤 | （无） |
| 运动 🟡 | （无） |
| 通勤 ⚪ | （无） |
| 娱乐 🩷 | （无） |
| 其他 ⬛ | 规则部分析事件 / 违反规则部的事件 / 整理学习资料等 |
| 睡觉 🔵 | 小憩 |

> 沿用二级分类时，**名称与颜色必须取自二级**（已用 `categoryLabelFor()` 保证名称=颜色一致）。

### 2.4 计时器（`timer.js` / `timer.html`）
- **正计时 = 打点**：开始/停止自动写时间记录；开始前可选分类（一级+二级）。
- **倒计时**：结束后写时间记录；分类选择器与设置面板体系一致（一级大按钮 + 二级小按钮）。
- **输入方式**：三个真实 `<input>`（时/分/秒）用**系统键盘**（有真实光标，聚焦全选，→/Tab 跳段）。
  每个数字框上下各一个 **▲▼ 箭头**（长按连续增减，到头循环：时 0~99、分/秒 0~59），桌面还支持**滚轮**。
- **进入状态 / 学习模式**：倒计时 `时长 = 音乐时长`；音乐播完 → 自动转正计时。
- **计时器大数字颜色**：计时中且**已打标签**时，数字颜色 = 当前分类/二级主题色；没打标签保持默认。

### 2.5 静音 / 图书馆模式
- 静音或图书馆模式下音乐**不播**，但倒计时**照样正常完整进行**（不是 1 秒跳过）。
- 进入状态时提示清楚：`进入状态中（静音模式，无音乐）` / `（图书馆模式，无音乐）`。

### 2.6 标签仪表盘（底部抽屉，对标爱时间）
- 计时器下方**常驻标签条**：彩色圆点 + 当前分类名 + 标签（或「未打标签」）+ ✎ 编辑按钮。
- 点标签条弹出**完整底部抽屉**：一级分类 → 二级分类 → 标签 → 备注（备注可收起/展开）。
- 计时中可**实时修改**分类/标签/备注，改后同步到当前计时与最终记录。
- 一段计时结束若**没有标签** → 自动弹出抽屉补标签；返回结果按**精确 recId** 更新，绝不能用"最后一条记录"。

### 2.7 时间轴 / 24H 时钟（今日活动构成）
- 未计时时段**留白并提示「未记录」**（横向时间轴：斜纹灰色占位；24H 时钟：浅灰扇形，悬浮显示"未记录·xx分钟"）。

---

## 3. 已回答的方案决策（不用再纠结）

| 问题 | 结论 |
|---|---|
| 手机原生"时钟 App" | 网页做不到。用 PWA 站内闹钟（Notification + Web Audio 响铃）+ iOS 引导式访问锁屏保活 |
| 蓝牙耳机检测 | 做不到。防外放底线 = 全局静音 + 图书馆模式 |
| 同步选型 | Supabase 免费版 Realtime（免费 + 三端实时同跑）。不用 OneDrive（需 Azure、无实时推送） |
| 滚动动画库 | 不用库，自写 `reveal.js`（IntersectionObserver） |
| 图标 | Lucide（MIT）本地化 `assets/icons/*.svg`，`icon.js` 按 `data-icon` 注入，**禁运行时引 CDN** |
| 番茄钟 / 点块自动开计时 / 单块目标 | 已被用户取消，别再加回来 |

---

## 4. 文件地图（改这里）

| 文件 | 职责 |
|---|---|
| `static/js/config.js` | ⭐ 配置中心：Supabase key、作息边界、分类体系、主题色、常用标签 |
| `static/js/clock.js` | 实时北京时间时钟（全站顶部 #liveClock） |
| `static/js/blocks.js` | 大块判定：blockOf / currentKey / remainingSeconds / blockDurationSec |
| `static/js/store.js` | ⭐ 数据出入口（localStorage + BroadcastChannel + Supabase 三层），读写必须走它 |
| `static/js/ui.js` | 静音/图书馆模式、同步徽标、alert |
| `static/js/icon.js` | 图标注入器（data-icon → 本地 SVG） |
| `static/js/home.js` | 首页：倒计时 / 三块 / 饼图+时间轴 / 快速计时 |
| `static/js/timer.js` | 计时器：数字输入 / 正倒切换 / 图谱标签条 / 抽屉 / 标签写入 |
| `static/js/tasks.js` | 任务三视图 / 类目 / 块筛选 |
| `static/js/stats.js` | 统计热力图 / 分类明细 / 导出 |
| `timer.html` | 计时器页结构（含倒计时输入、标签条） |
| `static/css/theme.css` | 全部样式 |
| `sw.js` | PWA 离线缓存（改静态资源必须升 `CACHE` 版本号） |
| `sql/schema.sql` | Supabase 建表/补列 SQL |
| `langqian/` | 浪前成长角落（原创占位） |

> 页面脚本加载顺序（四页一致，勿乱）：
> `config.js → clock.js → blocks.js（首页/计时）→ store.js → ui.js → icon.js → reveal.js → 各页业务 js`；`sw.js` 在 HTML 末尾注册。

---

## 5. 给接手 AI 的开发指南（硬规则，防踩坑）

1. **保持纯静态零构建**。加任何库前问：用户日后能直接改吗？离线能用吗？
2. **时间判定只走 `clock.js` / `blocks.js`**（北京时间），业务代码不得直接 `new Date().getHours()` 判块。
3. **读写数据只走 `window.Store` API**，不直读 localStorage。
4. **给 `time_records` 加字段** → 同步改 `sql/schema.sql` 补列（这是跨设备同步丢字段的原因）。
5. **新增/修改静态资源**（js/css/图标/HTML）→ 升 `sw.js` 的 `CACHE` 版本号，否则用户刷新不生效（旧 SW 缓存）。
6. **新图标**：优先复用 `assets/icons/` 现有；缺的从 Lucide 仓库下载 SVG 放进去，`<span data-icon="名字">` 注入。
   ⚠️ 图标缺失会**静默留白**（控制台会有 `[icon]` 警告），新增 `data-icon` 后务必确认文件存在。
7. **改动后必做验证**：
   - 所有 `static/js/*.js` + 业务 js：`node --check 文件`（node 无注入可用其他查 JS 语法方式）
   - 本地 `http.server` 打开对应页面，Ctrl+Shift+R 硬刷（绕过旧 SW）
8. **三套系统边界不可乱**：①时间记录=主线 ②计时器=工具写入主线 ③三块=展示+吃饭按钮（写 meal）。三块系统不得直接改计时器状态或其他类记录。
9. **不搬运任何版权素材**：洛天依官方立绘/歌曲、浪前课程原文，一律原创/占位。
10. **跨零点逻辑警惕**：涉及"从 X 点到次日 Y 点"的时段判断，用**绝对时间**（Date 差值），别用"当天秒数"（换日会断）。
11. **同步防回环（Supabase Realtime）**：`static/js/store.js` 的 `setLocal` 支持第 4 参 `skipPush`。**从云端拉取后写本地**（`refreshFromSupabase` / `pullOnce`）时必须传 `skipPush=true`，否则"拉取→写本地→再推回云端→Realtime 再触发拉取"会形成死循环。BroadcastChannel / storage 事件收远端数据只 `emit`、不 push，天然安全。**回环只由 `setLocal(skipPush=false)`（真实本地操作）触发上行**。

---

## 6. 已实现 vs 待办

### 已做（最近）
- [x] 三块剩余时间黑色加粗；去掉块目标，改「本块区间共 x小时」
- [x] 早块 08:00 起、晚块 23:40 止
- [x] 加时自习（23:40 → 次日 07:00）倒计时显示
- [x] 静音/图书馆：音乐不播但倒计时完整进行 + 提示无音乐
- [x] 分类体系一级+二级；设置面板与抽屉体系统一
- [x] 标签仪表盘 = 常驻标签条 + 底部抽屉，计时中实时改，结束无标签自动弹出（精确 recId 更新）
- [x] 计时数字颜色随已打标签的主题色
- [x] 时间轴/24H 时钟：未记录时段留白提示「未记录」
- [x] 倒计时系统键盘输入（真实光标）+ ▲▼ 箭头（长按连发）+ 滚轮
- [x] 修复 icon.js 缺失图标静默问题并发控制台警告；补齐 dumbbell/gamepad-2/pencil
- [x] 修复 stop/finishCountdown 的 sub_category 丢失；日志抽屉错改"最后一条记录"改为 recId 定位
- [x] 跨设备同步上线：`config.js` 已填 URL + anon key，`sql/schema.sql` 已执行
- [x] 部署上线：已发布公网主站 + 刷题站（GitHub Actions push 自动部署）
- [x] 修复 store.js Supabase Realtime **回环死循环**：`setLocal` 增 `skipPush` 参，`refreshFromSupabase` 拉取本地写不回流
- [x] 清理残留：删除 `neike-tiku/.npmrc`（本地 workaround，非标准键）

### 待办（用户想要但还没做 / 未确认）
1. **真实初试日期**：`EXAM_DATE` 现占位 `2026-12-19`，需用户确认
2. 时间轴**拖拽调整 / 补记 / 删除单条记录**
3. 吃饭时长 UI 上可加减（现只有默认 30min）
4. 任务-时间记录联动（`TASKS_LINK_TO_TIME_RECORDS` 预留开关，默认关）
5. 可选：课程表换医院/学校真实作息、深色模式、原创虚拟歌姬插画
6. 可选：清理 `neike-tiku` 内 Cloudflare/vinext 残留（`app/`、`worker/`、`vite.config.ts`、`.openai/`、`next.config.ts` 及 dev/build 脚本依赖）——仅 GitHub Pages 部署则皆死代码，但删除会破坏 `pnpm dev/build` 脚本，需连同依赖一并处理

---

## 6.1 最近修复记录（简要，按 SW 缓存版本号归组）

> 改动会同步升 `sw.js` 的 `CACHE` 版本号（v15 起），据此可反查改动范围。

**v15（计时器逻辑修复）**
- `stop()` / `finishCountdown()` 写时间记录时 `sub_category` 被清空 → 改为带 `at.sub_category`（二级分类不再丢失）。
- 计时结束"无标签自动弹抽屉"的保存：由取"最后一条记录"改为用刚生成的 `recId` **精确定位**更新（避免并发写错条）。
- `bindTagDrawer()` 里 `tagBox` **未声明即用** → 抛 `ReferenceError` 中断 `bind()`，连带 `setInterval(tick)` 未启动（表现为计时数字不走）→ 补声明修复。
- 新增 `categoryLabelFor()`：倒计时 label 用二级分类名，与颜色一致。
- 删除废弃的浮动「打标签」按钮 `tagDrawerTrigger`（HTML+JS）。

**v16（倒计时输入增强）**
- 三个数字框改为真实 `<input>` 用系统键盘（真实光标、聚焦全选、→/Tab 跳段）。
- 每个框加 **▲▼ 箭头**（长按连发、到头循环：时 0~99、分/秒 0~59）+ 桌面**滚轮**调节。新增 `bindStepControls()`、`.ti-field`/`.ti-arrow` 样式。

**v17（图标缺失修复）**
- 补齐缺失图标：`dumbbell.svg`（运动/打卡）、`gamepad-2.svg`（娱乐）、`pencil.svg`（标签编辑）。
- `icon.js`：缺失/空图标文件时打印 `[icon]` 控制台警告，不再静默留白。

**v18（加时自习）**
- 首页三块：晚块结束后（`23:40`）到次日 `07:00` 进入加时自习，显示"距离早上7:00还剩…"并点亮「现在」。
- 判断用**绝对时间**（当天 23:40 → 次日 07:00），**跨 0 点不断**。

---

## 7. 已知风险 / 易错点（再次强调）

- `tagBox` 变量：曾在 `bindTagDrawer()` 里**未声明即用**致 `ReferenceError` 中断 `bind()`（连带 `setInterval(tick)`）——已修复。以后在此区域新增变量务必先声明。
- `sub_category` 曾因写记录时忘带 `at.sub_category` 而丢失——两个写入点（stop / finishCountdown）都要带上。
- **换日**：任何跨 0 点的时段计算用绝对时间。
- **旧 SW 缓存**：改完必升 `CACHE`，否则用户看到的还是老版本，会误以为“没生效”。

---

## 8. 用户偏好（沟通时注意）

- 简体中文，结构化（表格/清单），接受直白反馈。
- 苹果风卡片视觉；喜欢一次给「做了什么 + 改动文件 + 如何预览」的简洁结论文风。
- 习惯在 WorkBuddy 里边预览边提修改意见；本地预览服务 `127.0.0.1:8099` 常开。